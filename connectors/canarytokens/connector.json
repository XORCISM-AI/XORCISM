{
  "id": "canarytokens",
  "name": "CanaryTokens",
  "type": "import",
  "category": "Honeypot",
  "description": "Self-hostable honeytoken generator and reporting dashboard; demo version available at [CanaryTokens.org](https://canarytokens.org/generate). Tool: https://github.com/thinkst/canarytokens. [SCAFFOLD generated from XORCISM.TOOL #603 by tool_to_connector.py — implement run() in run.py to map CanaryTokens output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:canarytokens",
  "run": "run.py",
  "mapping": "canarytokens",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CanaryTokens output file to parse instead of running it live."
    }
  ]
}
