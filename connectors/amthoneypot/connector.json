{
  "id": "amthoneypot",
  "name": "AMTHoneypot",
  "type": "import",
  "category": "Honeypot",
  "description": "Honeypot for Intel's AMT Firmware Vulnerability CVE-2017-5689. Tool: https://github.com/packetflare/amthoneypot. [SCAFFOLD generated from XORCISM.TOOL #391 by tool_to_connector.py — implement run() in run.py to map AMTHoneypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:amthoneypot",
  "run": "run.py",
  "mapping": "amthoneypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved AMTHoneypot output file to parse instead of running it live."
    }
  ]
}
