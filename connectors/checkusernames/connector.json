{
  "id": "checkusernames",
  "name": "CheckUserNames",
  "type": "import",
  "category": "OSINT",
  "description": "Check username across multiple platforms Tool: https://checkusernames.com/. [SCAFFOLD generated from XORCISM.TOOL #660 by tool_to_connector.py — implement run() in run.py to map CheckUserNames output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:checkusernames",
  "run": "run.py",
  "mapping": "checkusernames",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CheckUserNames output file to parse instead of running it live."
    }
  ]
}
