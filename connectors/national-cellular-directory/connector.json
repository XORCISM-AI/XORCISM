{
  "id": "national-cellular-directory",
  "name": "National Cellular Directory",
  "type": "import",
  "category": "OSINT",
  "description": "Phone owner lookup Tool: https://www.nationalcellulardirectory.com/. [SCAFFOLD generated from XORCISM.TOOL #689 by tool_to_connector.py — implement run() in run.py to map National Cellular Directory output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:national-cellular-directory",
  "run": "run.py",
  "mapping": "national-cellular-directory",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved National Cellular Directory output file to parse instead of running it live."
    }
  ]
}
