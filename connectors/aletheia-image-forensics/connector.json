{
  "id": "aletheia-image-forensics",
  "name": "Aletheia (image forensics)",
  "type": "import",
  "category": "OSINT",
  "description": "Detect manipulated/AI-generated images Tool: https://www.aletheia.ai. [SCAFFOLD generated from XORCISM.TOOL #1018 by tool_to_connector.py — implement run() in run.py to map Aletheia (image forensics) output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:aletheia-image-forensics",
  "run": "run.py",
  "mapping": "aletheia-image-forensics",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Aletheia (image forensics) output file to parse instead of running it live."
    }
  ]
}
