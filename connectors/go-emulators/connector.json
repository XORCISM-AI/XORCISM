{
  "id": "go-emulators",
  "name": "go-emulators",
  "type": "import",
  "category": "Honeypot",
  "description": "Honeypot Golang emulators. Tool: https://github.com/kingtuna/go-emulators. [SCAFFOLD generated from XORCISM.TOOL #495 by tool_to_connector.py — implement run() in run.py to map go-emulators output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:go-emulators",
  "run": "run.py",
  "mapping": "go-emulators",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved go-emulators output file to parse instead of running it live."
    }
  ]
}
