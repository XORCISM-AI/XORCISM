{
  "id": "startpage",
  "name": "Startpage",
  "type": "import",
  "category": "OSINT",
  "description": "Privacy-focused search — Google results with no tracking Tool: https://www.startpage.com. [SCAFFOLD generated from XORCISM.TOOL #1178 by tool_to_connector.py — implement run() in run.py to map Startpage output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:startpage",
  "run": "run.py",
  "mapping": "startpage",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Startpage output file to parse instead of running it live."
    }
  ]
}
