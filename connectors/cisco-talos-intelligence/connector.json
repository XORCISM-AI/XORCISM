{
  "id": "cisco-talos-intelligence",
  "name": "Cisco Talos Intelligence",
  "type": "import",
  "category": "OSINT",
  "description": "IP and Domain Reputation Center for real-time detection Tool: https://talosintelligence.com/reputation_center. [SCAFFOLD generated from XORCISM.TOOL #1053 by tool_to_connector.py — implement run() in run.py to map Cisco Talos Intelligence output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:cisco-talos-intelligence",
  "run": "run.py",
  "mapping": "cisco-talos-intelligence",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Cisco Talos Intelligence output file to parse instead of running it live."
    }
  ]
}
