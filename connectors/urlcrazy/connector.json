{
  "id": "urlcrazy",
  "name": "URLCrazy",
  "type": "import",
  "category": "OSINT",
  "description": "Generate domain typos and check availability/registration Tool: https://github.com/urbanadventurer/urlcrazy. [SCAFFOLD generated from XORCISM.TOOL #1201 by tool_to_connector.py — implement run() in run.py to map URLCrazy output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:urlcrazy",
  "run": "run.py",
  "mapping": "urlcrazy",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved URLCrazy output file to parse instead of running it live."
    }
  ]
}
