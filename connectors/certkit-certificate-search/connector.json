{
  "id": "certkit-certificate-search",
  "name": "CertKit Certificate Search",
  "type": "import",
  "category": "OSINT",
  "description": "Fast search for public SSL/TLS certificate records Tool: https://www.certkit.io/tools/ct-logs/. [SCAFFOLD generated from XORCISM.TOOL #1048 by tool_to_connector.py — implement run() in run.py to map CertKit Certificate Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:certkit-certificate-search",
  "run": "run.py",
  "mapping": "certkit-certificate-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CertKit Certificate Search output file to parse instead of running it live."
    }
  ]
}
