{
  "id": "google-hacking-database-ghdb",
  "name": "Google Hacking Database (GHDB)",
  "type": "import",
  "category": "OSINT",
  "description": "Index of dorks for finding publicly exposed info Tool: https://www.exploit-db.com/google-hacking-database. [SCAFFOLD generated from XORCISM.TOOL #1095 by tool_to_connector.py — implement run() in run.py to map Google Hacking Database (GHDB) output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:google-hacking-database-ghdb",
  "run": "run.py",
  "mapping": "google-hacking-database-ghdb",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Google Hacking Database (GHDB) output file to parse instead of running it live."
    }
  ]
}
