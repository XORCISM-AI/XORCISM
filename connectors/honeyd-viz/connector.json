{
  "id": "honeyd-viz",
  "name": "Honeyd-Viz",
  "type": "import",
  "category": "Honeypot",
  "description": "Honeyd-Viz (Honeypot). Tool: https://bruteforcelab.com/honeyd-viz. [SCAFFOLD generated from XORCISM.TOOL #611 by tool_to_connector.py — implement run() in run.py to map Honeyd-Viz output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:honeyd-viz",
  "run": "run.py",
  "mapping": "honeyd-viz",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Honeyd-Viz output file to parse instead of running it live."
    }
  ]
}
