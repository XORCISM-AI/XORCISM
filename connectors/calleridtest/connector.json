{
  "id": "calleridtest",
  "name": "CallerIDTest",
  "type": "import",
  "category": "OSINT",
  "description": "Reverse phone lookup Tool: https://calleridtest.com/. [SCAFFOLD generated from XORCISM.TOOL #687 by tool_to_connector.py — implement run() in run.py to map CallerIDTest output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:calleridtest",
  "run": "run.py",
  "mapping": "calleridtest",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CallerIDTest output file to parse instead of running it live."
    }
  ]
}
