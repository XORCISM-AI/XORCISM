{
  "id": "gxss",
  "name": "Gxss",
  "type": "import",
  "category": "OSINT",
  "description": "Test parameters for cross-site scripting reflection Tool: https://github.com/KathanP19/Gxss. [SCAFFOLD generated from XORCISM.TOOL #937 by tool_to_connector.py — implement run() in run.py to map Gxss output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:gxss",
  "run": "run.py",
  "mapping": "gxss",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Gxss output file to parse instead of running it live."
    }
  ]
}
