{
  "id": "evilginx",
  "name": "Evilginx",
  "type": "import",
  "category": "Phishing",
  "description": "Man-in-the-middle phishing framework able to bypass two-factor auth. Tool: https://github.com/kgretzky/evilginx2. [SCAFFOLD generated from XORCISM.TOOL #157 by tool_to_connector.py — implement run() in run.py to map Evilginx output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:evilginx",
  "run": "run.py",
  "mapping": "evilginx",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Evilginx output file to parse instead of running it live."
    }
  ]
}
