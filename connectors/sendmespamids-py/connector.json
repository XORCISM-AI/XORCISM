{
  "id": "sendmespamids-py",
  "name": "SendMeSpamIDS.py",
  "type": "import",
  "category": "Honeypot",
  "description": "Simple SMTP fetch all IDS and analyzer. Tool: https://github.com/johestephan/VerySimpleHoneypot. [SCAFFOLD generated from XORCISM.TOOL #581 by tool_to_connector.py — implement run() in run.py to map SendMeSpamIDS.py output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:sendmespamids-py",
  "run": "run.py",
  "mapping": "sendmespamids-py",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved SendMeSpamIDS.py output file to parse instead of running it live."
    }
  ]
}
