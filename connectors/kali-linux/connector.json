{
  "id": "kali-linux",
  "name": "Kali Linux",
  "type": "import",
  "category": "OSINT",
  "description": "Penetration testing (600+ tools) Tool: https://kali.org/. [SCAFFOLD generated from XORCISM.TOOL #903 by tool_to_connector.py — implement run() in run.py to map Kali Linux output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:kali-linux",
  "run": "run.py",
  "mapping": "kali-linux",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kali Linux output file to parse instead of running it live."
    }
  ]
}
