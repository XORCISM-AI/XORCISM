{
  "id": "foller-me",
  "name": "Foller.me",
  "type": "import",
  "category": "OSINT",
  "description": "Twitter analytics — bio, languages, hashtags, mentions Tool: https://foller.me. [SCAFFOLD generated from XORCISM.TOOL #1083 by tool_to_connector.py — implement run() in run.py to map Foller.me output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:foller-me",
  "run": "run.py",
  "mapping": "foller-me",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Foller.me output file to parse instead of running it live."
    }
  ]
}
