{
  "id": "cewl",
  "name": "CeWL",
  "type": "import",
  "category": "OSINT",
  "description": "Custom wordlist generator from website Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #783 by tool_to_connector.py — implement run() in run.py to map CeWL output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:cewl",
  "run": "run.py",
  "mapping": "cewl",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CeWL output file to parse instead of running it live."
    }
  ]
}
