{
  "id": "invid-weverify",
  "name": "InVID/WeVerify",
  "type": "import",
  "category": "OSINT",
  "description": "Video/image verification Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #929 by tool_to_connector.py — implement run() in run.py to map InVID/WeVerify output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:invid-weverify",
  "run": "run.py",
  "mapping": "invid-weverify",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved InVID/WeVerify output file to parse instead of running it live."
    }
  ]
}
