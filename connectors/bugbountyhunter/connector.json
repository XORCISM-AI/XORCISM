{
  "id": "bugbountyhunter",
  "name": "BugBountyHunter",
  "type": "import",
  "category": "OSINT",
  "description": "Web hacking training and labs Tool: https://www.bugbountyhunter.com. [SCAFFOLD generated from XORCISM.TOOL #983 by tool_to_connector.py — implement run() in run.py to map BugBountyHunter output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:bugbountyhunter",
  "run": "run.py",
  "mapping": "bugbountyhunter",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved BugBountyHunter output file to parse instead of running it live."
    }
  ]
}
