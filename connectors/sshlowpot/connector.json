{
  "id": "sshlowpot",
  "name": "sshlowpot",
  "type": "import",
  "category": "Honeypot",
  "description": "Yet another no-frills low-interaction SSH honeypot in Go. Tool: https://github.com/magisterquis/sshlowpot. [SCAFFOLD generated from XORCISM.TOOL #568 by tool_to_connector.py — implement run() in run.py to map sshlowpot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:sshlowpot",
  "run": "run.py",
  "mapping": "sshlowpot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved sshlowpot output file to parse instead of running it live."
    }
  ]
}
