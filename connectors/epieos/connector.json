{
  "id": "epieos",
  "name": "Epieos",
  "type": "import",
  "category": "Email Address OSINT",
  "description": "Email Address OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/epieos. [SCAFFOLD generated from XORCISM.TOOL #205 by tool_to_connector.py — implement run() in run.py to map Epieos output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:epieos",
  "run": "run.py",
  "mapping": "epieos",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Epieos output file to parse instead of running it live."
    }
  ]
}
