{
  "id": "spamd",
  "name": "spamd",
  "type": "import",
  "category": "Honeypot",
  "description": "spamd (Honeypot). Tool: http://man.openbsd.org/cgi-bin/man.cgi?query=spamd%26apropos=0%26sektion=0%26manpath=OpenBSD+Current%26arch=i386%26format=html. [SCAFFOLD generated from XORCISM.TOOL #588 by tool_to_connector.py — implement run() in run.py to map spamd output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:spamd",
  "run": "run.py",
  "mapping": "spamd",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved spamd output file to parse instead of running it live."
    }
  ]
}
