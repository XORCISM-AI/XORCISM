{
  "id": "dnspy",
  "name": "dnSpy",
  "type": "import",
  "category": "Reverse Engineering",
  "description": ".NET debugger and assembly editor/decompiler. Tool: https://github.com/dnSpy/dnSpy. [SCAFFOLD generated from XORCISM.TOOL #116 by tool_to_connector.py — implement run() in run.py to map dnSpy output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:dnspy",
  "run": "run.py",
  "mapping": "dnspy",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved dnSpy output file to parse instead of running it live."
    }
  ]
}
