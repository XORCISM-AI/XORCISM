{
  "id": "who-owns-what",
  "name": "Who Owns What",
  "type": "import",
  "category": "OSINT",
  "description": "NYC landlord portfolio lookup Tool: https://whoownswhat.justfix.org/en/. [SCAFFOLD generated from XORCISM.TOOL #859 by tool_to_connector.py — implement run() in run.py to map Who Owns What output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:who-owns-what",
  "run": "run.py",
  "mapping": "who-owns-what",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Who Owns What output file to parse instead of running it live."
    }
  ]
}
