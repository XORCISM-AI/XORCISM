{
  "id": "google-alerts",
  "name": "Google Alerts",
  "type": "import",
  "category": "OSINT",
  "description": "Free web monitoring Tool: https://www.google.com/alerts. [SCAFFOLD generated from XORCISM.TOOL #880 by tool_to_connector.py — implement run() in run.py to map Google Alerts output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:google-alerts",
  "run": "run.py",
  "mapping": "google-alerts",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Google Alerts output file to parse instead of running it live."
    }
  ]
}
