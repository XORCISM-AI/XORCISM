{
  "id": "phind",
  "name": "Phind",
  "type": "import",
  "category": "OSINT",
  "description": "AI search for developers Tool: https://www.phind.com/. [SCAFFOLD generated from XORCISM.TOOL #844 by tool_to_connector.py — implement run() in run.py to map Phind output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:phind",
  "run": "run.py",
  "mapping": "phind",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Phind output file to parse instead of running it live."
    }
  ]
}
