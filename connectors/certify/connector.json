{
  "id": "certify",
  "name": "Certify",
  "type": "import",
  "category": "Active Directory",
  "description": "Enumerates and abuses Active Directory Certificate Services (AD CS). Tool: https://github.com/GhostPack/Certify. [SCAFFOLD generated from XORCISM.TOOL #84 by tool_to_connector.py — implement run() in run.py to map Certify output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:certify",
  "run": "run.py",
  "mapping": "certify",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Certify output file to parse instead of running it live."
    }
  ]
}
