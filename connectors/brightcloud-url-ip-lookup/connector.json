{
  "id": "brightcloud-url-ip-lookup",
  "name": "BrightCloud URL/IP Lookup",
  "type": "import",
  "category": "OSINT",
  "description": "Reputation, category, and threat checks for URLs/IPs Tool: https://brightcloud.com/tools/url-ip-lookup.php. [SCAFFOLD generated from XORCISM.TOOL #1043 by tool_to_connector.py — implement run() in run.py to map BrightCloud URL/IP Lookup output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:brightcloud-url-ip-lookup",
  "run": "run.py",
  "mapping": "brightcloud-url-ip-lookup",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved BrightCloud URL/IP Lookup output file to parse instead of running it live."
    }
  ]
}
