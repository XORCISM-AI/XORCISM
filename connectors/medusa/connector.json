{
  "id": "medusa",
  "name": "Medusa",
  "type": "import",
  "category": "Password Cracking",
  "description": "Speedy, modular, parallel network login brute-forcer. Tool: https://github.com/jmk-foofus/medusa. [SCAFFOLD generated from XORCISM.TOOL #70 by tool_to_connector.py — implement run() in run.py to map Medusa output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:medusa",
  "run": "run.py",
  "mapping": "medusa",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Medusa output file to parse instead of running it live."
    }
  ]
}
