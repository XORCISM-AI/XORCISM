{
  "id": "interlace",
  "name": "Interlace",
  "type": "import",
  "category": "OSINT",
  "description": "Easily turn single-threaded CLI apps into multi-threaded jobs Tool: https://github.com/codingo/Interlace. [SCAFFOLD generated from XORCISM.TOOL #1110 by tool_to_connector.py — implement run() in run.py to map Interlace output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:interlace",
  "run": "run.py",
  "mapping": "interlace",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Interlace output file to parse instead of running it live."
    }
  ]
}
