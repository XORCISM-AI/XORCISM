{
  "id": "notebooklm",
  "name": "NotebookLM",
  "type": "import",
  "category": "OSINT",
  "description": "Google's document analysis AI Tool: https://notebooklm.google.com/. [SCAFFOLD generated from XORCISM.TOOL #843 by tool_to_connector.py — implement run() in run.py to map NotebookLM output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:notebooklm",
  "run": "run.py",
  "mapping": "notebooklm",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved NotebookLM output file to parse instead of running it live."
    }
  ]
}
