{
  "id": "publicwww",
  "name": "PublicWWW",
  "type": "import",
  "category": "OSINT",
  "description": "Search the web's source code (HTML/JS/CSS) for snippets, trackers, scripts Tool: https://publicwww.com. [SCAFFOLD generated from XORCISM.TOOL #1156 by tool_to_connector.py — implement run() in run.py to map PublicWWW output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:publicwww",
  "run": "run.py",
  "mapping": "publicwww",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PublicWWW output file to parse instead of running it live."
    }
  ]
}
