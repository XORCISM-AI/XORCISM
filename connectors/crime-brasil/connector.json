{
  "id": "crime-brasil",
  "name": "Crime Brasil",
  "type": "import",
  "category": "OSINT",
  "description": "Brazil crime + accidents open data by neighborhood (bairro-level RS; municipal MG/RJ; PRF accidents) Tool: https://crimebrasil.com.br. [SCAFFOLD generated from XORCISM.TOOL #720 by tool_to_connector.py — implement run() in run.py to map Crime Brasil output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:crime-brasil",
  "run": "run.py",
  "mapping": "crime-brasil",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Crime Brasil output file to parse instead of running it live."
    }
  ]
}
