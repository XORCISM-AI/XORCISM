{
  "id": "digital-shadows-searchlight",
  "name": "Digital Shadows SearchLight",
  "type": "import",
  "category": "OSINT",
  "description": "Brand protection + dark web monitoring (now ReliaQuest GreyMatter DRP) Tool: https://www.reliaquest.com. [SCAFFOLD generated from XORCISM.TOOL #962 by tool_to_connector.py — implement run() in run.py to map Digital Shadows SearchLight output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:digital-shadows-searchlight",
  "run": "run.py",
  "mapping": "digital-shadows-searchlight",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Digital Shadows SearchLight output file to parse instead of running it live."
    }
  ]
}
