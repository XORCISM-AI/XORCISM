{
  "id": "duckduckgo-onion",
  "name": "DuckDuckGo Onion",
  "type": "import",
  "category": "OSINT",
  "description": "Private search on Tor Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #789 by tool_to_connector.py — implement run() in run.py to map DuckDuckGo Onion output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:duckduckgo-onion",
  "run": "run.py",
  "mapping": "duckduckgo-onion",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DuckDuckGo Onion output file to parse instead of running it live."
    }
  ]
}
