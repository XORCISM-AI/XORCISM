{
  "id": "protonmail",
  "name": "ProtonMail",
  "type": "import",
  "category": "OSINT",
  "description": "Encrypted email Tool: https://proton.me/. [SCAFFOLD generated from XORCISM.TOOL #799 by tool_to_connector.py — implement run() in run.py to map ProtonMail output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:protonmail",
  "run": "run.py",
  "mapping": "protonmail",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ProtonMail output file to parse instead of running it live."
    }
  ]
}
