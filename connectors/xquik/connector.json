{
  "id": "xquik",
  "name": "Xquik",
  "type": "import",
  "category": "OSINT",
  "description": "X/Twitter data API & MCP skill Tool: https://github.com/Xquik-dev/x-twitter-scraper. [SCAFFOLD generated from XORCISM.TOOL #752 by tool_to_connector.py — implement run() in run.py to map Xquik output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:xquik",
  "run": "run.py",
  "mapping": "xquik",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Xquik output file to parse instead of running it live."
    }
  ]
}
