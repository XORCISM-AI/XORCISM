{
  "id": "domaineye-reverse-whois",
  "name": "DomainEye Reverse WHOIS",
  "type": "import",
  "category": "OSINT",
  "description": "Search domains by registrant data Tool: https://domaineye.com/reverse-whois. [SCAFFOLD generated from XORCISM.TOOL #1068 by tool_to_connector.py — implement run() in run.py to map DomainEye Reverse WHOIS output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:domaineye-reverse-whois",
  "run": "run.py",
  "mapping": "domaineye-reverse-whois",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DomainEye Reverse WHOIS output file to parse instead of running it live."
    }
  ]
}
