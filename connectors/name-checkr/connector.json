{
  "id": "name-checkr",
  "name": "Name Checkr",
  "type": "import",
  "category": "OSINT",
  "description": "Check domain and username across many platforms Tool: https://www.namecheckr.com. [SCAFFOLD generated from XORCISM.TOOL #1131 by tool_to_connector.py — implement run() in run.py to map Name Checkr output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:name-checkr",
  "run": "run.py",
  "mapping": "name-checkr",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Name Checkr output file to parse instead of running it live."
    }
  ]
}
