{
  "id": "castrick",
  "name": "Castrick",
  "type": "import",
  "category": "OSINT",
  "description": "Find social media accounts via email, username, phone Tool: https://castrickclues.com. [SCAFFOLD generated from XORCISM.TOOL #1046 by tool_to_connector.py — implement run() in run.py to map Castrick output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:castrick",
  "run": "run.py",
  "mapping": "castrick",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Castrick output file to parse instead of running it live."
    }
  ]
}
