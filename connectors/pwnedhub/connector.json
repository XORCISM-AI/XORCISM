{
  "id": "pwnedhub",
  "name": "PwnedHub",
  "type": "import",
  "category": "OSINT",
  "description": "Intentionally-vulnerable web app for hands-on appsec training Tool: https://github.com/practisec/pwnedhub. [SCAFFOLD generated from XORCISM.TOOL #999 by tool_to_connector.py — implement run() in run.py to map PwnedHub output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:pwnedhub",
  "run": "run.py",
  "mapping": "pwnedhub",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PwnedHub output file to parse instead of running it live."
    }
  ]
}
