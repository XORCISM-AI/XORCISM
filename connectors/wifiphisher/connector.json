{
  "id": "wifiphisher",
  "name": "Wifiphisher",
  "type": "import",
  "category": "Wireless",
  "description": "Rogue access point framework for red-team WiFi attacks. Tool: https://github.com/wifiphisher/wifiphisher. [SCAFFOLD generated from XORCISM.TOOL #90 by tool_to_connector.py — implement run() in run.py to map Wifiphisher output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:wifiphisher",
  "run": "run.py",
  "mapping": "wifiphisher",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Wifiphisher output file to parse instead of running it live."
    }
  ]
}
