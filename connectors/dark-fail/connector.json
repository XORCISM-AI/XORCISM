{
  "id": "dark-fail",
  "name": "Dark.fail",
  "type": "import",
  "category": "OSINT",
  "description": "Verified dark web links directory Tool: https://dark.fail/. [SCAFFOLD generated from XORCISM.TOOL #793 by tool_to_connector.py — implement run() in run.py to map Dark.fail output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:dark-fail",
  "run": "run.py",
  "mapping": "dark-fail",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Dark.fail output file to parse instead of running it live."
    }
  ]
}
