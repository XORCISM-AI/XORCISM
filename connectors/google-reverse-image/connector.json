{
  "id": "google-reverse-image",
  "name": "Google Reverse Image",
  "type": "import",
  "category": "OSINT",
  "description": "Google image search Tool: https://images.google.com/. [SCAFFOLD generated from XORCISM.TOOL #721 by tool_to_connector.py — implement run() in run.py to map Google Reverse Image output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:google-reverse-image",
  "run": "run.py",
  "mapping": "google-reverse-image",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Google Reverse Image output file to parse instead of running it live."
    }
  ]
}
