{
  "id": "numverify",
  "name": "NumVerify",
  "type": "import",
  "category": "OSINT",
  "description": "Phone number validation API Tool: https://numverify.com/. [SCAFFOLD generated from XORCISM.TOOL #684 by tool_to_connector.py — implement run() in run.py to map NumVerify output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:numverify",
  "run": "run.py",
  "mapping": "numverify",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved NumVerify output file to parse instead of running it live."
    }
  ]
}
