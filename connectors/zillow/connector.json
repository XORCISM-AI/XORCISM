{
  "id": "zillow",
  "name": "Zillow",
  "type": "import",
  "category": "OSINT",
  "description": "Property records & estimates Tool: https://zillow.com/. [SCAFFOLD generated from XORCISM.TOOL #858 by tool_to_connector.py — implement run() in run.py to map Zillow output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:zillow",
  "run": "run.py",
  "mapping": "zillow",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Zillow output file to parse instead of running it live."
    }
  ]
}
