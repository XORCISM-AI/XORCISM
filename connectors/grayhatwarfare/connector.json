{
  "id": "grayhatwarfare",
  "name": "GrayhatWarfare",
  "type": "import",
  "category": "OSINT",
  "description": "Index of open Amazon S3 buckets — find exposed cloud data Tool: https://grayhatwarfare.com. [SCAFFOLD generated from XORCISM.TOOL #1098 by tool_to_connector.py — implement run() in run.py to map GrayhatWarfare output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:grayhatwarfare",
  "run": "run.py",
  "mapping": "grayhatwarfare",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved GrayhatWarfare output file to parse instead of running it live."
    }
  ]
}
