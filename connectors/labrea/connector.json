{
  "id": "labrea",
  "name": "LaBrea",
  "type": "import",
  "category": "Honeypot",
  "description": "Takes over unused IP addresses, and creates virtual servers that are attractive to worms, hackers, and other denizens of the Internet. Tool: http://labrea.sourceforge.net/labrea-info.html. [SCAFFOLD generated from XORCISM.TOOL #485 by tool_to_connector.py — implement run() in run.py to map LaBrea output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:labrea",
  "run": "run.py",
  "mapping": "labrea",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved LaBrea output file to parse instead of running it live."
    }
  ]
}
