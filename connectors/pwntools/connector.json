{
  "id": "pwntools",
  "name": "pwntools",
  "type": "import",
  "category": "Exploitation",
  "description": "CTF framework and exploit-development library written in Python. Tool: https://github.com/Gallopsled/pwntools. [SCAFFOLD generated from XORCISM.TOOL #66 by tool_to_connector.py — implement run() in run.py to map pwntools output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:pwntools",
  "run": "run.py",
  "mapping": "pwntools",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved pwntools output file to parse instead of running it live."
    }
  ]
}
