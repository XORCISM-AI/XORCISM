{
  "id": "known-agents",
  "name": "Known Agents",
  "type": "import",
  "category": "Foundational OSINT",
  "description": "Foundational OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/known-agents. [SCAFFOLD generated from XORCISM.TOOL #256 by tool_to_connector.py — implement run() in run.py to map Known Agents output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:known-agents",
  "run": "run.py",
  "mapping": "known-agents",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Known Agents output file to parse instead of running it live."
    }
  ]
}
