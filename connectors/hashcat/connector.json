{
  "id": "hashcat",
  "name": "Hashcat",
  "type": "import",
  "category": "Password Cracking",
  "description": "World's fastest GPU-accelerated password recovery tool. Tool: https://hashcat.net/hashcat/. [SCAFFOLD generated from XORCISM.TOOL #68 by tool_to_connector.py — implement run() in run.py to map Hashcat output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:hashcat",
  "run": "run.py",
  "mapping": "hashcat",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Hashcat output file to parse instead of running it live."
    }
  ]
}
