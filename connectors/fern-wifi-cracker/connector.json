{
  "id": "fern-wifi-cracker",
  "name": "Fern Wifi Cracker",
  "type": "import",
  "category": "OSINT",
  "description": "GUI-based Wi-Fi audit tool Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #828 by tool_to_connector.py — implement run() in run.py to map Fern Wifi Cracker output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:fern-wifi-cracker",
  "run": "run.py",
  "mapping": "fern-wifi-cracker",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Fern Wifi Cracker output file to parse instead of running it live."
    }
  ]
}
