{
  "id": "bettercap",
  "name": "Bettercap",
  "type": "import",
  "category": "MITM",
  "description": "Swiss-army knife for network attacks and monitoring over WiFi, BLE and Ethernet. Tool: https://www.bettercap.org. [SCAFFOLD generated from XORCISM.TOOL #17 by tool_to_connector.py — implement run() in run.py to map Bettercap output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:bettercap",
  "run": "run.py",
  "mapping": "bettercap",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Bettercap output file to parse instead of running it live."
    }
  ]
}
