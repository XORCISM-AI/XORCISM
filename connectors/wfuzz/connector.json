{
  "id": "wfuzz",
  "name": "Wfuzz",
  "type": "import",
  "category": "Web Application",
  "description": "Web application fuzzer for brute-forcing parameters, directories and more. Tool: https://github.com/xmendez/wfuzz. [SCAFFOLD generated from XORCISM.TOOL #38 by tool_to_connector.py — implement run() in run.py to map Wfuzz output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:wfuzz",
  "run": "run.py",
  "mapping": "wfuzz",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Wfuzz output file to parse instead of running it live."
    }
  ]
}
