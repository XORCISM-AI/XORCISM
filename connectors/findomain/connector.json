{
  "id": "findomain",
  "name": "Findomain",
  "type": "import",
  "category": "OSINT",
  "description": "Cross-platform subdomain enumerator with monitoring features Tool: https://github.com/Findomain/Findomain. [SCAFFOLD generated from XORCISM.TOOL #1081 by tool_to_connector.py — implement run() in run.py to map Findomain output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:findomain",
  "run": "run.py",
  "mapping": "findomain",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Findomain output file to parse instead of running it live."
    }
  ]
}
