{
  "id": "hcxtools",
  "name": "hcxtools",
  "type": "import",
  "category": "OSINT",
  "description": "Wi-Fi packet capture conversion Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #830 by tool_to_connector.py — implement run() in run.py to map hcxtools output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hcxtools",
  "run": "run.py",
  "mapping": "hcxtools",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved hcxtools output file to parse instead of running it live."
    }
  ]
}
