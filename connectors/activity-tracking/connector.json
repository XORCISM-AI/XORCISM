{
  "id": "activity-tracking",
  "name": "Activity tracking",
  "type": "import",
  "category": "OSINT",
  "description": "nebaz.ru Tool: https://nebaz.ru/. [SCAFFOLD generated from XORCISM.TOOL #876 by tool_to_connector.py — implement run() in run.py to map Activity tracking output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:activity-tracking",
  "run": "run.py",
  "mapping": "activity-tracking",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Activity tracking output file to parse instead of running it live."
    }
  ]
}
