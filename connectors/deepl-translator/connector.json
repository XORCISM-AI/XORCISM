{
  "id": "deepl-translator",
  "name": "DeepL Translator",
  "type": "import",
  "category": "Language Translation OSINT",
  "description": "Language Translation OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/deepl-translator. [SCAFFOLD generated from XORCISM.TOOL #299 by tool_to_connector.py — implement run() in run.py to map DeepL Translator output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:deepl-translator",
  "run": "run.py",
  "mapping": "deepl-translator",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DeepL Translator output file to parse instead of running it live."
    }
  ]
}
