{
  "id": "yumpu",
  "name": "Yumpu",
  "type": "import",
  "category": "OSINT",
  "description": "Document publishing — sometimes leaks via search Tool: https://www.yumpu.com. [SCAFFOLD generated from XORCISM.TOOL #1218 by tool_to_connector.py — implement run() in run.py to map Yumpu output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:yumpu",
  "run": "run.py",
  "mapping": "yumpu",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Yumpu output file to parse instead of running it live."
    }
  ]
}
