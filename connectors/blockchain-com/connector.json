{
  "id": "blockchain-com",
  "name": "Blockchain.com",
  "type": "import",
  "category": "OSINT",
  "description": "Bitcoin explorer Tool: https://blockchain.com/explorer. [SCAFFOLD generated from XORCISM.TOOL #850 by tool_to_connector.py — implement run() in run.py to map Blockchain.com output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:blockchain-com",
  "run": "run.py",
  "mapping": "blockchain-com",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Blockchain.com output file to parse instead of running it live."
    }
  ]
}
