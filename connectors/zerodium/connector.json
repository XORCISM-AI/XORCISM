{
  "id": "zerodium",
  "name": "Zerodium",
  "type": "import",
  "category": "OSINT",
  "description": "Premium 0day acquisition platform Tool: https://zerodium.com. [SCAFFOLD generated from XORCISM.TOOL #1015 by tool_to_connector.py — implement run() in run.py to map Zerodium output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:zerodium",
  "run": "run.py",
  "mapping": "zerodium",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Zerodium output file to parse instead of running it live."
    }
  ]
}
