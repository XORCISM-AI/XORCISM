{
  "id": "court-listener",
  "name": "Court Listener",
  "type": "import",
  "category": "OSINT",
  "description": "Free US court opinion search Tool: https://courtlistener.com/. [SCAFFOLD generated from XORCISM.TOOL #861 by tool_to_connector.py — implement run() in run.py to map Court Listener output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:court-listener",
  "run": "run.py",
  "mapping": "court-listener",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Court Listener output file to parse instead of running it live."
    }
  ]
}
