{
  "id": "brute-ratel",
  "name": "Brute Ratel",
  "type": "import",
  "category": "C2",
  "description": "Commercial red-team C2 and adversary-simulation toolkit. Tool: https://bruteratel.com. [SCAFFOLD generated from XORCISM.TOOL #62 by tool_to_connector.py — implement run() in run.py to map Brute Ratel output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:brute-ratel",
  "run": "run.py",
  "mapping": "brute-ratel",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Brute Ratel output file to parse instead of running it live."
    }
  ]
}
