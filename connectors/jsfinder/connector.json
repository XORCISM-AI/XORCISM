{
  "id": "jsfinder",
  "name": "JSFinder",
  "type": "import",
  "category": "OSINT",
  "description": "Find JS files referenced from a URL — endpoints / API paths Tool: https://github.com/Threezh1/JSFinder. [SCAFFOLD generated from XORCISM.TOOL #1112 by tool_to_connector.py — implement run() in run.py to map JSFinder output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:jsfinder",
  "run": "run.py",
  "mapping": "jsfinder",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved JSFinder output file to parse instead of running it live."
    }
  ]
}
