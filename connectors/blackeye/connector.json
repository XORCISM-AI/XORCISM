{
  "id": "blackeye",
  "name": "BlackEye",
  "type": "import",
  "category": "OSINT",
  "description": "32+ phishing template builder for credential capture (lab-only) Tool: https://github.com/An0nUD4Y/blackeye. [SCAFFOLD generated from XORCISM.TOOL #1037 by tool_to_connector.py — implement run() in run.py to map BlackEye output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:blackeye",
  "run": "run.py",
  "mapping": "blackeye",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved BlackEye output file to parse instead of running it live."
    }
  ]
}
