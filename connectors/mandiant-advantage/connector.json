{
  "id": "mandiant-advantage",
  "name": "Mandiant Advantage",
  "type": "import",
  "category": "OSINT",
  "description": "Threat intel from Google's Mandiant team Tool: https://www.mandiant.com/advantage. [SCAFFOLD generated from XORCISM.TOOL #968 by tool_to_connector.py — implement run() in run.py to map Mandiant Advantage output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:mandiant-advantage",
  "run": "run.py",
  "mapping": "mandiant-advantage",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Mandiant Advantage output file to parse instead of running it live."
    }
  ]
}
