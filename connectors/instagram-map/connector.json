{
  "id": "instagram-map",
  "name": "Instagram Map",
  "type": "import",
  "category": "Social Media OSINT",
  "description": "Social Media OSINT — Instagram. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/instagram-map. [SCAFFOLD generated from XORCISM.TOOL #305 by tool_to_connector.py — implement run() in run.py to map Instagram Map output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:instagram-map",
  "run": "run.py",
  "mapping": "instagram-map",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Instagram Map output file to parse instead of running it live."
    }
  ]
}
