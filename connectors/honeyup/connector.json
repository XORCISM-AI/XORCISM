{
  "id": "honeyup",
  "name": "honeyup",
  "type": "import",
  "category": "Honeypot",
  "description": "An uploader honeypot designed to look like poor website security. Tool: https://github.com/LogoiLab/honeyup. [SCAFFOLD generated from XORCISM.TOOL #373 by tool_to_connector.py — implement run() in run.py to map honeyup output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:honeyup",
  "run": "run.py",
  "mapping": "honeyup",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved honeyup output file to parse instead of running it live."
    }
  ]
}
