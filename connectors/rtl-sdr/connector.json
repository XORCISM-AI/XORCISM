{
  "id": "rtl-sdr",
  "name": "RTL-SDR",
  "type": "import",
  "category": "OSINT",
  "description": "Budget software-defined radio dongle Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #900 by tool_to_connector.py — implement run() in run.py to map RTL-SDR output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:rtl-sdr",
  "run": "run.py",
  "mapping": "rtl-sdr",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved RTL-SDR output file to parse instead of running it live."
    }
  ]
}
