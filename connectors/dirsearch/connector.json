{
  "id": "dirsearch",
  "name": "dirsearch",
  "type": "import",
  "category": "OSINT",
  "description": "Web path scanner — advanced wordlist-based directory bruteforce Tool: https://pypi.org/project/dirsearch/. [SCAFFOLD generated from XORCISM.TOOL #1064 by tool_to_connector.py — implement run() in run.py to map dirsearch output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:dirsearch",
  "run": "run.py",
  "mapping": "dirsearch",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved dirsearch output file to parse instead of running it live."
    }
  ]
}
