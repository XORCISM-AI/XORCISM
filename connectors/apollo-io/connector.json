{
  "id": "apollo-io",
  "name": "Apollo.io",
  "type": "import",
  "category": "OSINT",
  "description": "B2B phone/email finder — 1200 free credits/yr Tool: https://www.apollo.io. [SCAFFOLD generated from XORCISM.TOOL #1022 by tool_to_connector.py — implement run() in run.py to map Apollo.io output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:apollo-io",
  "run": "run.py",
  "mapping": "apollo-io",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Apollo.io output file to parse instead of running it live."
    }
  ]
}
