{
  "id": "kippo-junos",
  "name": "Kippo_JunOS",
  "type": "import",
  "category": "Honeypot",
  "description": "Kippo configured to be a backdoored netscreen. Tool: https://github.com/gregcmartin/Kippo_JunOS. [SCAFFOLD generated from XORCISM.TOOL #546 by tool_to_connector.py — implement run() in run.py to map Kippo_JunOS output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:kippo-junos",
  "run": "run.py",
  "mapping": "kippo-junos",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kippo_JunOS output file to parse instead of running it live."
    }
  ]
}
