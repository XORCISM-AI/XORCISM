{
  "id": "beenverified",
  "name": "BeenVerified",
  "type": "import",
  "category": "OSINT",
  "description": "People search & background check Tool: https://beenverified.com/. [SCAFFOLD generated from XORCISM.TOOL #739 by tool_to_connector.py — implement run() in run.py to map BeenVerified output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:beenverified",
  "run": "run.py",
  "mapping": "beenverified",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved BeenVerified output file to parse instead of running it live."
    }
  ]
}
