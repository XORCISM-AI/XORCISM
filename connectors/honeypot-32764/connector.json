{
  "id": "honeypot-32764",
  "name": "Honeypot-32764",
  "type": "import",
  "category": "Honeypot",
  "description": "Honeypot for router backdoor (TCP 32764). Tool: https://github.com/knalli/honeypot-for-tcp-32764. [SCAFFOLD generated from XORCISM.TOOL #455 by tool_to_connector.py — implement run() in run.py to map Honeypot-32764 output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:honeypot-32764",
  "run": "run.py",
  "mapping": "honeypot-32764",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Honeypot-32764 output file to parse instead of running it live."
    }
  ]
}
