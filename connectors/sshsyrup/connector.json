{
  "id": "sshsyrup",
  "name": "sshsyrup",
  "type": "import",
  "category": "Honeypot",
  "description": "Simple SSH Honeypot with features to capture terminal activity and upload to asciinema.org. Tool: https://github.com/mkishere/sshsyrup. [SCAFFOLD generated from XORCISM.TOOL #569 by tool_to_connector.py — implement run() in run.py to map sshsyrup output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:sshsyrup",
  "run": "run.py",
  "mapping": "sshsyrup",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved sshsyrup output file to parse instead of running it live."
    }
  ]
}
