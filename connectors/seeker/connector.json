{
  "id": "seeker",
  "name": "Seeker",
  "type": "import",
  "category": "OSINT",
  "description": "Geolocation by phishing — collects precise GPS via WebRTC + browser geolocation Tool: https://github.com/thewhiteh4t/seeker. [SCAFFOLD generated from XORCISM.TOOL #1169 by tool_to_connector.py — implement run() in run.py to map Seeker output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:seeker",
  "run": "run.py",
  "mapping": "seeker",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Seeker output file to parse instead of running it live."
    }
  ]
}
