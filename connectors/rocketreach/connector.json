{
  "id": "rocketreach",
  "name": "RocketReach",
  "type": "import",
  "category": "OSINT",
  "description": "Find professional emails/phones for 700M+ profiles Tool: https://rocketreach.co. [SCAFFOLD generated from XORCISM.TOOL #1165 by tool_to_connector.py — implement run() in run.py to map RocketReach output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:rocketreach",
  "run": "run.py",
  "mapping": "rocketreach",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved RocketReach output file to parse instead of running it live."
    }
  ]
}
