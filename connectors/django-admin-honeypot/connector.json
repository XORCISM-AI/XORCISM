{
  "id": "django-admin-honeypot",
  "name": "django-admin-honeypot",
  "type": "import",
  "category": "Honeypot",
  "description": "Fake Django admin login screen to notify admins of attempted unauthorized access. Tool: https://github.com/dmpayton/django-admin-honeypot. [SCAFFOLD generated from XORCISM.TOOL #368 by tool_to_connector.py — implement run() in run.py to map django-admin-honeypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:django-admin-honeypot",
  "run": "run.py",
  "mapping": "django-admin-honeypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved django-admin-honeypot output file to parse instead of running it live."
    }
  ]
}
