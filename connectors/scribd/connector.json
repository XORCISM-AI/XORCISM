{
  "id": "scribd",
  "name": "Scribd",
  "type": "import",
  "category": "OSINT",
  "description": "Search for documents, presentations, sheet music, ebooks Tool: https://www.scribd.com. [SCAFFOLD generated from XORCISM.TOOL #1167 by tool_to_connector.py — implement run() in run.py to map Scribd output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:scribd",
  "run": "run.py",
  "mapping": "scribd",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Scribd output file to parse instead of running it live."
    }
  ]
}
