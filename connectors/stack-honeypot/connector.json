{
  "id": "stack-honeypot",
  "name": "stack-honeypot",
  "type": "import",
  "category": "Honeypot",
  "description": "Inserts a trap for spam bots into responses. Tool: https://github.com/CHH/stack-honeypot. [SCAFFOLD generated from XORCISM.TOOL #382 by tool_to_connector.py — implement run() in run.py to map stack-honeypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:stack-honeypot",
  "run": "run.py",
  "mapping": "stack-honeypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved stack-honeypot output file to parse instead of running it live."
    }
  ]
}
