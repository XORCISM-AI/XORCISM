{
  "id": "innerwarden",
  "name": "InnerWarden",
  "type": "import",
  "category": "Honeypot",
  "description": "Security agent with built-in SSH and HTTP honeypots featuring an LLM-powered interactive fake shell that captures credentials and attacker commands. Tool: https://github.com/InnerWarden/innerwarden. [SCAFFOLD generated from XORCISM.TOOL #544 by tool_to_connector.py — implement run() in run.py to map InnerWarden output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:innerwarden",
  "run": "run.py",
  "mapping": "innerwarden",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved InnerWarden output file to parse instead of running it live."
    }
  ]
}
