{
  "id": "discordhistory",
  "name": "DiscordHistory",
  "type": "import",
  "category": "OSINT",
  "description": "Search public Discord servers and messages Tool: https://disboard.org. [SCAFFOLD generated from XORCISM.TOOL #1066 by tool_to_connector.py — implement run() in run.py to map DiscordHistory output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:discordhistory",
  "run": "run.py",
  "mapping": "discordhistory",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DiscordHistory output file to parse instead of running it live."
    }
  ]
}
