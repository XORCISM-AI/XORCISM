{
  "id": "digital-footprint-check",
  "name": "Digital Footprint Check",
  "type": "import",
  "category": "OSINT",
  "description": "Free username check on hundreds of sites Tool: https://www.digitalfootprintcheck.com/free-checker.html. [SCAFFOLD generated from XORCISM.TOOL #1063 by tool_to_connector.py — implement run() in run.py to map Digital Footprint Check output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:digital-footprint-check",
  "run": "run.py",
  "mapping": "digital-footprint-check",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Digital Footprint Check output file to parse instead of running it live."
    }
  ]
}
