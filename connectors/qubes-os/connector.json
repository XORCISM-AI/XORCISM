{
  "id": "qubes-os",
  "name": "Qubes OS",
  "type": "import",
  "category": "OSINT",
  "description": "Security via compartmentalization Tool: https://qubes-os.org/. [SCAFFOLD generated from XORCISM.TOOL #911 by tool_to_connector.py — implement run() in run.py to map Qubes OS output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:qubes-os",
  "run": "run.py",
  "mapping": "qubes-os",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Qubes OS output file to parse instead of running it live."
    }
  ]
}
