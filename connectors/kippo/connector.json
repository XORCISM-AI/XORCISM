{
  "id": "kippo",
  "name": "Kippo",
  "type": "import",
  "category": "Honeypot",
  "description": "Medium interaction SSH honeypot. Tool: https://github.com/desaster/kippo. [SCAFFOLD generated from XORCISM.TOOL #545 by tool_to_connector.py — implement run() in run.py to map Kippo output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:kippo",
  "run": "run.py",
  "mapping": "kippo",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kippo output file to parse instead of running it live."
    }
  ]
}
