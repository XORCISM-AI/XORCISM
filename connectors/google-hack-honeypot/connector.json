{
  "id": "google-hack-honeypot",
  "name": "Google Hack Honeypot",
  "type": "import",
  "category": "Honeypot",
  "description": "Designed to provide reconnaissance against attackers that use search engines as a hacking tool against your resources. Tool: http://ghh.sourceforge.net. [SCAFFOLD generated from XORCISM.TOOL #356 by tool_to_connector.py — implement run() in run.py to map Google Hack Honeypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:google-hack-honeypot",
  "run": "run.py",
  "mapping": "google-hack-honeypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Google Hack Honeypot output file to parse instead of running it live."
    }
  ]
}
