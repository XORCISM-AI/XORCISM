{
  "id": "dshield-docker",
  "name": "DShield docker",
  "type": "import",
  "category": "Honeypot",
  "description": "Docker container running cowrie with DShield output enabled. Tool: https://github.com/xme/dshield-docker. [SCAFFOLD generated from XORCISM.TOOL #540 by tool_to_connector.py — implement run() in run.py to map DShield docker output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:dshield-docker",
  "run": "run.py",
  "mapping": "dshield-docker",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DShield docker output file to parse instead of running it live."
    }
  ]
}
