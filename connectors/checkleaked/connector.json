{
  "id": "checkleaked",
  "name": "CheckLeaked",
  "type": "import",
  "category": "OSINT",
  "description": "Leak search engine (15B+ accounts) Tool: https://checkleaked.cc/. [SCAFFOLD generated from XORCISM.TOOL #763 by tool_to_connector.py — implement run() in run.py to map CheckLeaked output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:checkleaked",
  "run": "run.py",
  "mapping": "checkleaked",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CheckLeaked output file to parse instead of running it live."
    }
  ]
}
