{
  "id": "foaf-data",
  "name": "FOAF data",
  "type": "import",
  "category": "OSINT",
  "description": "https://vk.com/foaf.php?id=USER_ID Tool: https://vk.com/foaf.php?id=USER_ID. [SCAFFOLD generated from XORCISM.TOOL #874 by tool_to_connector.py — implement run() in run.py to map FOAF data output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:foaf-data",
  "run": "run.py",
  "mapping": "foaf-data",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved FOAF data output file to parse instead of running it live."
    }
  ]
}
