{
  "id": "hive-moderation",
  "name": "Hive Moderation",
  "type": "import",
  "category": "OSINT",
  "description": "AI content detection Tool: https://hivemoderation.com/. [SCAFFOLD generated from XORCISM.TOOL #730 by tool_to_connector.py — implement run() in run.py to map Hive Moderation output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hive-moderation",
  "run": "run.py",
  "mapping": "hive-moderation",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Hive Moderation output file to parse instead of running it live."
    }
  ]
}
