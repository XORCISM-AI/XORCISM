{
  "id": "getnotify",
  "name": "GetNotify",
  "type": "import",
  "category": "OSINT",
  "description": "Email open tracking + geolocation Tool: https://getnotify.com/. [SCAFFOLD generated from XORCISM.TOOL #675 by tool_to_connector.py — implement run() in run.py to map GetNotify output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:getnotify",
  "run": "run.py",
  "mapping": "getnotify",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved GetNotify output file to parse instead of running it live."
    }
  ]
}
