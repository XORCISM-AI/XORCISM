{
  "id": "twiteur",
  "name": "Twiteur",
  "type": "import",
  "category": "OSINT",
  "description": "Twitter/X advanced search wrapper — geo, date, sentiment Tool: https://twiteur.com. [SCAFFOLD generated from XORCISM.TOOL #1198 by tool_to_connector.py — implement run() in run.py to map Twiteur output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:twiteur",
  "run": "run.py",
  "mapping": "twiteur",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Twiteur output file to parse instead of running it live."
    }
  ]
}
