{
  "id": "public-transport-maps",
  "name": "Public Transport Maps",
  "type": "import",
  "category": "OSINT",
  "description": "20+ online public transport maps by country Tool: https://cipher387.github.io/public_transport_maps/. [SCAFFOLD generated from XORCISM.TOOL #1155 by tool_to_connector.py — implement run() in run.py to map Public Transport Maps output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:public-transport-maps",
  "run": "run.py",
  "mapping": "public-transport-maps",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Public Transport Maps output file to parse instead of running it live."
    }
  ]
}
