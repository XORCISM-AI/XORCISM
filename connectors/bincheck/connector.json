{
  "id": "bincheck",
  "name": "BinCheck",
  "type": "import",
  "category": "OSINT",
  "description": "Bank card BIN lookup Tool: https://bincheck.io/. [SCAFFOLD generated from XORCISM.TOOL #852 by tool_to_connector.py — implement run() in run.py to map BinCheck output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:bincheck",
  "run": "run.py",
  "mapping": "bincheck",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved BinCheck output file to parse instead of running it live."
    }
  ]
}
