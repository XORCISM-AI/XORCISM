{
  "id": "domain-digger",
  "name": "Domain Digger",
  "type": "import",
  "category": "Network Infrastructure OSINT",
  "description": "Network Infrastructure OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/domain-digger. [SCAFFOLD generated from XORCISM.TOOL #236 by tool_to_connector.py — implement run() in run.py to map Domain Digger output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:domain-digger",
  "run": "run.py",
  "mapping": "domain-digger",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Domain Digger output file to parse instead of running it live."
    }
  ]
}
