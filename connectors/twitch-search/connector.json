{
  "id": "twitch-search",
  "name": "Twitch Search",
  "type": "import",
  "category": "OSINT",
  "description": "Search live streams by category, language, viewer count Tool: https://www.twitch.tv/directory. [SCAFFOLD generated from XORCISM.TOOL #1197 by tool_to_connector.py — implement run() in run.py to map Twitch Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:twitch-search",
  "run": "run.py",
  "mapping": "twitch-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Twitch Search output file to parse instead of running it live."
    }
  ]
}
