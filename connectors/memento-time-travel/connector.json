{
  "id": "memento-time-travel",
  "name": "Memento Time Travel",
  "type": "import",
  "category": "OSINT",
  "description": "Federated search across multiple web archives Tool: https://timetravel.mementoweb.org. [SCAFFOLD generated from XORCISM.TOOL #1122 by tool_to_connector.py — implement run() in run.py to map Memento Time Travel output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:memento-time-travel",
  "run": "run.py",
  "mapping": "memento-time-travel",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Memento Time Travel output file to parse instead of running it live."
    }
  ]
}
