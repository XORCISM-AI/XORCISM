{
  "id": "hlr-lookups",
  "name": "Hlr-Lookups",
  "type": "import",
  "category": "OSINT",
  "description": "HLR phone number lookup Tool: https://www.hlr-lookups.com/. [SCAFFOLD generated from XORCISM.TOOL #692 by tool_to_connector.py — implement run() in run.py to map Hlr-Lookups output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hlr-lookups",
  "run": "run.py",
  "mapping": "hlr-lookups",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Hlr-Lookups output file to parse instead of running it live."
    }
  ]
}
