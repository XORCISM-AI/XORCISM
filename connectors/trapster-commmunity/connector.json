{
  "id": "trapster-commmunity",
  "name": "Trapster Commmunity",
  "type": "import",
  "category": "Honeypot",
  "description": "Modural and easy to install Python Honeypot, with comprehensive alerting Tool: https://github.com/0xBallpoint/trapster-community. [SCAFFOLD generated from XORCISM.TOOL #417 by tool_to_connector.py — implement run() in run.py to map Trapster Commmunity output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:trapster-commmunity",
  "run": "run.py",
  "mapping": "trapster-commmunity",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Trapster Commmunity output file to parse instead of running it live."
    }
  ]
}
