{
  "id": "kismet",
  "name": "Kismet",
  "type": "import",
  "category": "Wireless",
  "description": "Wireless network and device detector, sniffer and intrusion detection system. Tool: https://www.kismetwireless.net. [SCAFFOLD generated from XORCISM.TOOL #87 by tool_to_connector.py — implement run() in run.py to map Kismet output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:kismet",
  "run": "run.py",
  "mapping": "kismet",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kismet output file to parse instead of running it live."
    }
  ]
}
