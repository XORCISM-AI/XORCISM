{
  "id": "volatility",
  "name": "Volatility",
  "type": "import",
  "category": "Memory Forensics",
  "description": "Advanced memory-forensics framework for RAM analysis. Tool: https://github.com/volatilityfoundation/volatility3. [SCAFFOLD generated from XORCISM.TOOL #96 by tool_to_connector.py — implement run() in run.py to map Volatility output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:volatility",
  "run": "run.py",
  "mapping": "volatility",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Volatility output file to parse instead of running it live."
    }
  ]
}
