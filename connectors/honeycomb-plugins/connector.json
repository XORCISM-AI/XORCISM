{
  "id": "honeycomb-plugins",
  "name": "honeycomb_plugins",
  "type": "import",
  "category": "Honeypot",
  "description": "Plugin repository for Honeycomb, the honeypot framework by Cymmetria. Tool: https://github.com/Cymmetria/honeycomb_plugins. [SCAFFOLD generated from XORCISM.TOOL #399 by tool_to_connector.py — implement run() in run.py to map honeycomb_plugins output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:honeycomb-plugins",
  "run": "run.py",
  "mapping": "honeycomb-plugins",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved honeycomb_plugins output file to parse instead of running it live."
    }
  ]
}
