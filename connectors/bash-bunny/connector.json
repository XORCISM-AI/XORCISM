{
  "id": "bash-bunny",
  "name": "Bash Bunny",
  "type": "import",
  "category": "OSINT",
  "description": "Multi-function USB attack platform Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #898 by tool_to_connector.py — implement run() in run.py to map Bash Bunny output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:bash-bunny",
  "run": "run.py",
  "mapping": "bash-bunny",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Bash Bunny output file to parse instead of running it live."
    }
  ]
}
