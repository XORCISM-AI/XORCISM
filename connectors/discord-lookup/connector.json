{
  "id": "discord-lookup",
  "name": "Discord Lookup",
  "type": "import",
  "category": "OSINT",
  "description": "Look up Discord user info via ID Tool: https://discordlookup.com. [SCAFFOLD generated from XORCISM.TOOL #1065 by tool_to_connector.py — implement run() in run.py to map Discord Lookup output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:discord-lookup",
  "run": "run.py",
  "mapping": "discord-lookup",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Discord Lookup output file to parse instead of running it live."
    }
  ]
}
