{
  "id": "hackerone",
  "name": "HackerOne",
  "type": "import",
  "category": "OSINT",
  "description": "Largest bug-bounty platform — public and private programs Tool: https://hackerone.com. [SCAFFOLD generated from XORCISM.TOOL #1007 by tool_to_connector.py — implement run() in run.py to map HackerOne output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hackerone",
  "run": "run.py",
  "mapping": "hackerone",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved HackerOne output file to parse instead of running it live."
    }
  ]
}
