{
  "id": "ignorant",
  "name": "Ignorant",
  "type": "import",
  "category": "OSINT",
  "description": "Check phone registrations on sites Tool: https://pypi.org/project/ignorant/. [SCAFFOLD generated from XORCISM.TOOL #682 by tool_to_connector.py — implement run() in run.py to map Ignorant output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:ignorant",
  "run": "run.py",
  "mapping": "ignorant",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Ignorant output file to parse instead of running it live."
    }
  ]
}
