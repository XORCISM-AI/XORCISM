{
  "id": "phpmyadmin-honeypot",
  "name": "phpmyadmin_honeypot",
  "type": "import",
  "category": "Honeypot",
  "description": "Simple and effective phpMyAdmin honeypot. Tool: https://github.com/gfoss/phpmyadmin_honeypot. [SCAFFOLD generated from XORCISM.TOOL #377 by tool_to_connector.py — implement run() in run.py to map phpmyadmin_honeypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:phpmyadmin-honeypot",
  "run": "run.py",
  "mapping": "phpmyadmin-honeypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved phpmyadmin_honeypot output file to parse instead of running it live."
    }
  ]
}
