{
  "id": "teleteg",
  "name": "Teleteg",
  "type": "import",
  "category": "OSINT",
  "description": "Telegram search engine — 10 free results Tool: https://teleteg.com. [SCAFFOLD generated from XORCISM.TOOL #1185 by tool_to_connector.py — implement run() in run.py to map Teleteg output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:teleteg",
  "run": "run.py",
  "mapping": "teleteg",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Teleteg output file to parse instead of running it live."
    }
  ]
}
