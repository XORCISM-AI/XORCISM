{
  "id": "portlurker",
  "name": "portlurker",
  "type": "import",
  "category": "Honeypot",
  "description": "Port listener in Rust with protocol guessing and safe string display. Tool: https://github.com/bartnv/portlurker. [SCAFFOLD generated from XORCISM.TOOL #500 by tool_to_connector.py — implement run() in run.py to map portlurker output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:portlurker",
  "run": "run.py",
  "mapping": "portlurker",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved portlurker output file to parse instead of running it live."
    }
  ]
}
