{
  "id": "crackmapexec",
  "name": "CrackMapExec",
  "type": "import",
  "category": "Credential Access",
  "description": "Swiss-army knife for pentesting Active Directory networks. Tool: https://github.com/Porchetta-Industries/CrackMapExec. [SCAFFOLD generated from XORCISM.TOOL #74 by tool_to_connector.py — implement run() in run.py to map CrackMapExec output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:crackmapexec",
  "run": "run.py",
  "mapping": "crackmapexec",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CrackMapExec output file to parse instead of running it live."
    }
  ]
}
