{
  "id": "ssh-auth-logger",
  "name": "ssh-auth-logger",
  "type": "import",
  "category": "Honeypot",
  "description": "Low/zero interaction SSH authentication logging honeypot. Tool: https://github.com/JustinAzoff/ssh-auth-logger. [SCAFFOLD generated from XORCISM.TOOL #562 by tool_to_connector.py — implement run() in run.py to map ssh-auth-logger output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:ssh-auth-logger",
  "run": "run.py",
  "mapping": "ssh-auth-logger",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ssh-auth-logger output file to parse instead of running it live."
    }
  ]
}
