{
  "id": "file-phish",
  "name": "File Phish",
  "type": "import",
  "category": "Cyberthreat Intelligence OSINT",
  "description": "Cyberthreat Intelligence OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/file-phish. [SCAFFOLD generated from XORCISM.TOOL #243 by tool_to_connector.py — implement run() in run.py to map File Phish output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:file-phish",
  "run": "run.py",
  "mapping": "file-phish",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved File Phish output file to parse instead of running it live."
    }
  ]
}
