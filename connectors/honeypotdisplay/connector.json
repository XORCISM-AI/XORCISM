{
  "id": "honeypotdisplay",
  "name": "honeypotDisplay",
  "type": "import",
  "category": "Honeypot",
  "description": "Flask website which displays data gathered from an SSH Honeypot. Tool: https://github.com/Joss-Steward/honeypotDisplay. [SCAFFOLD generated from XORCISM.TOOL #631 by tool_to_connector.py — implement run() in run.py to map honeypotDisplay output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:honeypotdisplay",
  "run": "run.py",
  "mapping": "honeypotdisplay",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved honeypotDisplay output file to parse instead of running it live."
    }
  ]
}
