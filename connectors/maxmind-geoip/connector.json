{
  "id": "maxmind-geoip",
  "name": "MaxMind GeoIP",
  "type": "import",
  "category": "OSINT",
  "description": "IP geolocation database Tool: https://maxmind.com/. [SCAFFOLD generated from XORCISM.TOOL #868 by tool_to_connector.py — implement run() in run.py to map MaxMind GeoIP output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:maxmind-geoip",
  "run": "run.py",
  "mapping": "maxmind-geoip",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved MaxMind GeoIP output file to parse instead of running it live."
    }
  ]
}
