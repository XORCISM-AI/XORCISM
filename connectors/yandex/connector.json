{
  "id": "yandex",
  "name": "Yandex",
  "type": "import",
  "category": "OSINT",
  "description": "Russian search engine — strong reverse-image and Cyrillic support Tool: https://www.yandex.com. [SCAFFOLD generated from XORCISM.TOOL #1216 by tool_to_connector.py — implement run() in run.py to map Yandex output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:yandex",
  "run": "run.py",
  "mapping": "yandex",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Yandex output file to parse instead of running it live."
    }
  ]
}
