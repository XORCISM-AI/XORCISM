{
  "id": "getcontact",
  "name": "GetContact",
  "type": "import",
  "category": "OSINT",
  "description": "See how number is saved by others Tool: https://getcontact.com/. [SCAFFOLD generated from XORCISM.TOOL #683 by tool_to_connector.py — implement run() in run.py to map GetContact output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:getcontact",
  "run": "run.py",
  "mapping": "getcontact",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved GetContact output file to parse instead of running it live."
    }
  ]
}
