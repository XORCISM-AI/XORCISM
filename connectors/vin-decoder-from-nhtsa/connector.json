{
  "id": "vin-decoder-from-nhtsa",
  "name": "VIN Decoder from NHTSA",
  "type": "import",
  "category": "Transport OSINT",
  "description": "Transport OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/vin-decoder-from-nhtsa. [SCAFFOLD generated from XORCISM.TOOL #290 by tool_to_connector.py — implement run() in run.py to map VIN Decoder from NHTSA output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:vin-decoder-from-nhtsa",
  "run": "run.py",
  "mapping": "vin-decoder-from-nhtsa",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved VIN Decoder from NHTSA output file to parse instead of running it live."
    }
  ]
}
