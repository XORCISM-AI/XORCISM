{
  "id": "powershell-empire",
  "name": "PowerShell Empire",
  "type": "import",
  "category": "C2",
  "description": "Post-exploitation C2 framework with PowerShell and Python agents. Tool: https://github.com/BC-SECURITY/Empire. [SCAFFOLD generated from XORCISM.TOOL #58 by tool_to_connector.py — implement run() in run.py to map PowerShell Empire output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:powershell-empire",
  "run": "run.py",
  "mapping": "powershell-empire",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PowerShell Empire output file to parse instead of running it live."
    }
  ]
}
