{
  "id": "dshield-web-honeypot-project",
  "name": "DShield Web Honeypot Project",
  "type": "import",
  "category": "Honeypot",
  "description": "DShield Web Honeypot Project (Honeypot). Tool: https://sites.google.com/site/webhoneypotsite/. [SCAFFOLD generated from XORCISM.TOOL #571 by tool_to_connector.py — implement run() in run.py to map DShield Web Honeypot Project output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:dshield-web-honeypot-project",
  "run": "run.py",
  "mapping": "dshield-web-honeypot-project",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DShield Web Honeypot Project output file to parse instead of running it live."
    }
  ]
}
