{
  "id": "the-intercept",
  "name": "The Intercept",
  "type": "import",
  "category": "OSINT",
  "description": "Investigative journalism Tool: https://theintercept.com/. [SCAFFOLD generated from XORCISM.TOOL #774 by tool_to_connector.py — implement run() in run.py to map The Intercept output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:the-intercept",
  "run": "run.py",
  "mapping": "the-intercept",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved The Intercept output file to parse instead of running it live."
    }
  ]
}
