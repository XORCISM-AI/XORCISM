{
  "id": "phobos",
  "name": "Phobos",
  "type": "import",
  "category": "OSINT",
  "description": "Dark web search engine Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #790 by tool_to_connector.py — implement run() in run.py to map Phobos output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:phobos",
  "run": "run.py",
  "mapping": "phobos",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Phobos output file to parse instead of running it live."
    }
  ]
}
