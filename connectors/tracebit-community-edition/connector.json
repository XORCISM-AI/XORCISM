{
  "id": "tracebit-community-edition",
  "name": "Tracebit Community Edition",
  "type": "import",
  "category": "Honeypot",
  "description": "online free canary tokens (AWS, SSH, Email, Web) Tool: https://community.tracebit.com. [SCAFFOLD generated from XORCISM.TOOL #608 by tool_to_connector.py — implement run() in run.py to map Tracebit Community Edition output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:tracebit-community-edition",
  "run": "run.py",
  "mapping": "tracebit-community-edition",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Tracebit Community Edition output file to parse instead of running it live."
    }
  ]
}
