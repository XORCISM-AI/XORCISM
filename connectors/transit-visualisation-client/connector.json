{
  "id": "transit-visualisation-client",
  "name": "Transit Visualisation Client",
  "type": "import",
  "category": "OSINT",
  "description": "Real-time public transport across 700+ cities Tool: https://tracker.geops.ch. [SCAFFOLD generated from XORCISM.TOOL #1193 by tool_to_connector.py — implement run() in run.py to map Transit Visualisation Client output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:transit-visualisation-client",
  "run": "run.py",
  "mapping": "transit-visualisation-client",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Transit Visualisation Client output file to parse instead of running it live."
    }
  ]
}
