{
  "id": "gau-getallurls",
  "name": "gau (getallurls)",
  "type": "import",
  "category": "OSINT",
  "description": "Fetch URLs from AlienVault OTX, Wayback, Common Crawl, URLScan Tool: https://github.com/lc/gau. [SCAFFOLD generated from XORCISM.TOOL #1086 by tool_to_connector.py — implement run() in run.py to map gau (getallurls) output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:gau-getallurls",
  "run": "run.py",
  "mapping": "gau-getallurls",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved gau (getallurls) output file to parse instead of running it live."
    }
  ]
}
