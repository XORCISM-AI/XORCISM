{
  "id": "pinterest",
  "name": "Pinterest",
  "type": "import",
  "category": "OSINT",
  "description": "Visual search engine — alternate reverse-image source Tool: https://www.pinterest.com. [SCAFFOLD generated from XORCISM.TOOL #1152 by tool_to_connector.py — implement run() in run.py to map Pinterest output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:pinterest",
  "run": "run.py",
  "mapping": "pinterest",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Pinterest output file to parse instead of running it live."
    }
  ]
}
