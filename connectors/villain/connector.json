{
  "id": "villain",
  "name": "Villain",
  "type": "import",
  "category": "OSINT",
  "description": "High-level stage 0/1 C2 framework for handling sibling agents Tool: https://github.com/t3l3machus/Villain. [SCAFFOLD generated from XORCISM.TOOL #949 by tool_to_connector.py — implement run() in run.py to map Villain output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:villain",
  "run": "run.py",
  "mapping": "villain",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Villain output file to parse instead of running it live."
    }
  ]
}
