{
  "id": "smart-honeypot",
  "name": "smart-honeypot",
  "type": "import",
  "category": "Honeypot",
  "description": "PHP Script demonstrating a smart honey pot. Tool: https://github.com/freak3dot/smart-honeypot. [SCAFFOLD generated from XORCISM.TOOL #379 by tool_to_connector.py — implement run() in run.py to map smart-honeypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:smart-honeypot",
  "run": "run.py",
  "mapping": "smart-honeypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved smart-honeypot output file to parse instead of running it live."
    }
  ]
}
