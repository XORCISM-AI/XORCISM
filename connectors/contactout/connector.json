{
  "id": "contactout",
  "name": "ContactOut",
  "type": "import",
  "category": "OSINT",
  "description": "Find emails and phones for 300M+ professionals Tool: https://contactout.com. [SCAFFOLD generated from XORCISM.TOOL #1059 by tool_to_connector.py — implement run() in run.py to map ContactOut output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:contactout",
  "run": "run.py",
  "mapping": "contactout",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ContactOut output file to parse instead of running it live."
    }
  ]
}
