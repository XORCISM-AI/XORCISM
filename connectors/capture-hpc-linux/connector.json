{
  "id": "capture-hpc-linux",
  "name": "Capture-HPC-Linux",
  "type": "import",
  "category": "Honeypot",
  "description": "Capture-HPC-Linux (Honeypot). Tool: https://redmine.honeynet.org/projects/linux-capture-hpc/wiki. [SCAFFOLD generated from XORCISM.TOOL #516 by tool_to_connector.py — implement run() in run.py to map Capture-HPC-Linux output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:capture-hpc-linux",
  "run": "run.py",
  "mapping": "capture-hpc-linux",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Capture-HPC-Linux output file to parse instead of running it live."
    }
  ]
}
