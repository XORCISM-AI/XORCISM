{
  "id": "bitquery",
  "name": "Bitquery",
  "type": "import",
  "category": "OSINT",
  "description": "Blockchain data APIs for on-chain investigation Tool: https://bitquery.io. [SCAFFOLD generated from XORCISM.TOOL #1035 by tool_to_connector.py — implement run() in run.py to map Bitquery output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:bitquery",
  "run": "run.py",
  "mapping": "bitquery",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Bitquery output file to parse instead of running it live."
    }
  ]
}
