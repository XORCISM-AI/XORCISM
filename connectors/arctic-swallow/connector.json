{
  "id": "arctic-swallow",
  "name": "arctic-swallow",
  "type": "import",
  "category": "Honeypot",
  "description": "Low interaction honeypot. Tool: https://github.com/ajackal/arctic-swallow. [SCAFFOLD generated from XORCISM.TOOL #491 by tool_to_connector.py — implement run() in run.py to map arctic-swallow output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:arctic-swallow",
  "run": "run.py",
  "mapping": "arctic-swallow",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved arctic-swallow output file to parse instead of running it live."
    }
  ]
}
