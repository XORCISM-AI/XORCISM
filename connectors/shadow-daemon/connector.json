{
  "id": "shadow-daemon",
  "name": "Shadow Daemon",
  "type": "import",
  "category": "Honeypot",
  "description": "Modular Web Application Firewall / High-Interaction Honeypot for PHP, Perl, and Python apps. Tool: https://shadowd.zecure.org/overview/introduction/. [SCAFFOLD generated from XORCISM.TOOL #363 by tool_to_connector.py — implement run() in run.py to map Shadow Daemon output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:shadow-daemon",
  "run": "run.py",
  "mapping": "shadow-daemon",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Shadow Daemon output file to parse instead of running it live."
    }
  ]
}
