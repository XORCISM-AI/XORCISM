{
  "id": "alfa-awus036ach",
  "name": "Alfa AWUS036ACH",
  "type": "import",
  "category": "OSINT",
  "description": "Long-range dual-band Wi-Fi adapter Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #901 by tool_to_connector.py — implement run() in run.py to map Alfa AWUS036ACH output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:alfa-awus036ach",
  "run": "run.py",
  "mapping": "alfa-awus036ach",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Alfa AWUS036ACH output file to parse instead of running it live."
    }
  ]
}
