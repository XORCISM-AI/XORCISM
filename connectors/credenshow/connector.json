{
  "id": "credenshow",
  "name": "CredenShow",
  "type": "import",
  "category": "OSINT",
  "description": "Identify your compromised credentials before others do Tool: https://credenshow.com. [SCAFFOLD generated from XORCISM.TOOL #1060 by tool_to_connector.py — implement run() in run.py to map CredenShow output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:credenshow",
  "run": "run.py",
  "mapping": "credenshow",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CredenShow output file to parse instead of running it live."
    }
  ]
}
