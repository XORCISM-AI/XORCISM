{
  "id": "telpoisk",
  "name": "TelPoisk",
  "type": "import",
  "category": "OSINT",
  "description": "Russian phone directory Tool: https://telpoisk.com/. [SCAFFOLD generated from XORCISM.TOOL #690 by tool_to_connector.py — implement run() in run.py to map TelPoisk output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:telpoisk",
  "run": "run.py",
  "mapping": "telpoisk",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved TelPoisk output file to parse instead of running it live."
    }
  ]
}
