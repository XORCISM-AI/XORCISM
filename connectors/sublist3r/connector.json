{
  "id": "sublist3r",
  "name": "Sublist3r",
  "type": "import",
  "category": "Recon",
  "description": "Subdomain enumeration tool that leverages OSINT search engines. Tool: https://github.com/aboul3la/Sublist3r. [SCAFFOLD generated from XORCISM.TOOL #49 by tool_to_connector.py — implement run() in run.py to map Sublist3r output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:sublist3r",
  "run": "run.py",
  "mapping": "sublist3r",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Sublist3r output file to parse instead of running it live."
    }
  ]
}
