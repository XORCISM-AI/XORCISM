{
  "id": "redishoneypot",
  "name": "RedisHoneyPot",
  "type": "import",
  "category": "Honeypot",
  "description": "High Interaction Honeypot Solution for Redis protocol. Tool: https://github.com/cypwnpwnsocute/RedisHoneyPot. [SCAFFOLD generated from XORCISM.TOOL #348 by tool_to_connector.py — implement run() in run.py to map RedisHoneyPot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:redishoneypot",
  "run": "run.py",
  "mapping": "redishoneypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved RedisHoneyPot output file to parse instead of running it live."
    }
  ]
}
