{
  "id": "snov-io",
  "name": "Snov.io",
  "type": "import",
  "category": "OSINT",
  "description": "Email finder & verifier Tool: https://snov.io/. [SCAFFOLD generated from XORCISM.TOOL #676 by tool_to_connector.py — implement run() in run.py to map Snov.io output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:snov-io",
  "run": "run.py",
  "mapping": "snov-io",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Snov.io output file to parse instead of running it live."
    }
  ]
}
