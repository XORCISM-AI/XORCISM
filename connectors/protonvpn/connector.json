{
  "id": "protonvpn",
  "name": "ProtonVPN",
  "type": "import",
  "category": "OSINT",
  "description": "Free encrypted VPN Tool: https://protonvpn.com/. [SCAFFOLD generated from XORCISM.TOOL #798 by tool_to_connector.py — implement run() in run.py to map ProtonVPN output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:protonvpn",
  "run": "run.py",
  "mapping": "protonvpn",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ProtonVPN output file to parse instead of running it live."
    }
  ]
}
