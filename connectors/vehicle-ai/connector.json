{
  "id": "vehicle-ai",
  "name": "Vehicle AI",
  "type": "import",
  "category": "Transport OSINT",
  "description": "Transport OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/vehicle-ai. [SCAFFOLD generated from XORCISM.TOOL #291 by tool_to_connector.py — implement run() in run.py to map Vehicle AI output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:vehicle-ai",
  "run": "run.py",
  "mapping": "vehicle-ai",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Vehicle AI output file to parse instead of running it live."
    }
  ]
}
