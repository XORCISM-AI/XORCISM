{
  "id": "walletexplorer",
  "name": "WalletExplorer",
  "type": "import",
  "category": "OSINT",
  "description": "Smart bitcoin block explorer — clusters addresses by entity Tool: https://walletexplorer.com. [SCAFFOLD generated from XORCISM.TOOL #1208 by tool_to_connector.py — implement run() in run.py to map WalletExplorer output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:walletexplorer",
  "run": "run.py",
  "mapping": "walletexplorer",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved WalletExplorer output file to parse instead of running it live."
    }
  ]
}
