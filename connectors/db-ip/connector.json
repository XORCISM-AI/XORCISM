{
  "id": "db-ip",
  "name": "DB-IP",
  "type": "import",
  "category": "OSINT",
  "description": "IP geolocation database Tool: https://db-ip.com/. [SCAFFOLD generated from XORCISM.TOOL #706 by tool_to_connector.py — implement run() in run.py to map DB-IP output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:db-ip",
  "run": "run.py",
  "mapping": "db-ip",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DB-IP output file to parse instead of running it live."
    }
  ]
}
