{
  "id": "waybackurls",
  "name": "waybackurls",
  "type": "import",
  "category": "OSINT",
  "description": "Fetch all URLs the Wayback Machine knows about a domain Tool: https://github.com/tomnomnom/waybackurls. [SCAFFOLD generated from XORCISM.TOOL #1209 by tool_to_connector.py — implement run() in run.py to map waybackurls output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:waybackurls",
  "run": "run.py",
  "mapping": "waybackurls",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved waybackurls output file to parse instead of running it live."
    }
  ]
}
