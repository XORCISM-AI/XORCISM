{
  "id": "onionland-search",
  "name": "OnionLand Search",
  "type": "import",
  "category": "OSINT",
  "description": "Search engine for dark-web content Tool: https://onionlandsearchengine.com. [SCAFFOLD generated from XORCISM.TOOL #1140 by tool_to_connector.py — implement run() in run.py to map OnionLand Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:onionland-search",
  "run": "run.py",
  "mapping": "onionland-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved OnionLand Search output file to parse instead of running it live."
    }
  ]
}
