{
  "id": "geocreepy",
  "name": "Geocreepy",
  "type": "import",
  "category": "OSINT",
  "description": "Geolocation aggregator — pulls geotagged posts across networks Tool: http://www.geocreepy.com. [SCAFFOLD generated from XORCISM.TOOL #1087 by tool_to_connector.py — implement run() in run.py to map Geocreepy output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:geocreepy",
  "run": "run.py",
  "mapping": "geocreepy",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Geocreepy output file to parse instead of running it live."
    }
  ]
}
