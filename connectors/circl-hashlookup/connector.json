{
  "id": "circl-hashlookup",
  "name": "CIRCL Hashlookup",
  "type": "import",
  "category": "OSINT",
  "description": "Free public hashlookup for known software files Tool: https://www.circl.lu/services/hashlookup. [SCAFFOLD generated from XORCISM.TOOL #1052 by tool_to_connector.py — implement run() in run.py to map CIRCL Hashlookup output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:circl-hashlookup",
  "run": "run.py",
  "mapping": "circl-hashlookup",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CIRCL Hashlookup output file to parse instead of running it live."
    }
  ]
}
