{
  "id": "that-s-them",
  "name": "That's Them",
  "type": "import",
  "category": "People OSINT",
  "description": "People OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/thats-them. [SCAFFOLD generated from XORCISM.TOOL #221 by tool_to_connector.py — implement run() in run.py to map That's Them output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:that-s-them",
  "run": "run.py",
  "mapping": "that-s-them",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved That's Them output file to parse instead of running it live."
    }
  ]
}
