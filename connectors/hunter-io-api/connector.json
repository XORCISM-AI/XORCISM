{
  "id": "hunter-io-api",
  "name": "Hunter.io API",
  "type": "import",
  "category": "OSINT",
  "description": "Email discovery Tool: https://hunter.io/api. [SCAFFOLD generated from XORCISM.TOOL #915 by tool_to_connector.py — implement run() in run.py to map Hunter.io API output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hunter-io-api",
  "run": "run.py",
  "mapping": "hunter-io-api",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Hunter.io API output file to parse instead of running it live."
    }
  ]
}
