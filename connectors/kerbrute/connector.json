{
  "id": "kerbrute",
  "name": "Kerbrute",
  "type": "import",
  "category": "Credential Access",
  "description": "Tool to brute-force and enumerate Active Directory accounts via Kerberos. Tool: https://github.com/ropnop/kerbrute. [SCAFFOLD generated from XORCISM.TOOL #77 by tool_to_connector.py — implement run() in run.py to map Kerbrute output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:kerbrute",
  "run": "run.py",
  "mapping": "kerbrute",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kerbrute output file to parse instead of running it live."
    }
  ]
}
