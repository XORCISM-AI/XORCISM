{
  "id": "user-searcher",
  "name": "User-Searcher",
  "type": "import",
  "category": "OSINT",
  "description": "Search a username across 2000+ websites Tool: https://www.user-searcher.com. [SCAFFOLD generated from XORCISM.TOOL #1203 by tool_to_connector.py — implement run() in run.py to map User-Searcher output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:user-searcher",
  "run": "run.py",
  "mapping": "user-searcher",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved User-Searcher output file to parse instead of running it live."
    }
  ]
}
