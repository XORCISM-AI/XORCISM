{
  "id": "sherlock",
  "name": "Sherlock",
  "type": "import",
  "category": "OSINT",
  "description": "Hunts down social-media accounts by username across many sites. Tool: https://github.com/sherlock-project/sherlock. [SCAFFOLD generated from XORCISM.TOOL #131 by tool_to_connector.py — implement run() in run.py to map Sherlock output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:sherlock",
  "run": "run.py",
  "mapping": "sherlock",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Sherlock output file to parse instead of running it live."
    }
  ]
}
