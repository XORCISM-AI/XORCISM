{
  "id": "photorec",
  "name": "PhotoRec",
  "type": "import",
  "category": "OSINT",
  "description": "File data recovery — focuses on multimedia Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #977 by tool_to_connector.py — implement run() in run.py to map PhotoRec output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:photorec",
  "run": "run.py",
  "mapping": "photorec",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PhotoRec output file to parse instead of running it live."
    }
  ]
}
