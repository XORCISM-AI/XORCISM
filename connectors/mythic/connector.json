{
  "id": "mythic",
  "name": "Mythic",
  "type": "import",
  "category": "C2",
  "description": "Collaborative, multi-agent red-team command-and-control framework. Tool: https://github.com/its-a-feature/Mythic. [SCAFFOLD generated from XORCISM.TOOL #60 by tool_to_connector.py — implement run() in run.py to map Mythic output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:mythic",
  "run": "run.py",
  "mapping": "mythic",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Mythic output file to parse instead of running it live."
    }
  ]
}
