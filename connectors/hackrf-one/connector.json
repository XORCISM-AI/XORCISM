{
  "id": "hackrf-one",
  "name": "HackRF One",
  "type": "import",
  "category": "OSINT",
  "description": "Software-defined radio (1MHz–6GHz) Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #894 by tool_to_connector.py — implement run() in run.py to map HackRF One output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hackrf-one",
  "run": "run.py",
  "mapping": "hackrf-one",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved HackRF One output file to parse instead of running it live."
    }
  ]
}
