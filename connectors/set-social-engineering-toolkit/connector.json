{
  "id": "set-social-engineering-toolkit",
  "name": "SET (Social Engineering Toolkit)",
  "type": "import",
  "category": "OSINT",
  "description": "Complete SE framework Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #814 by tool_to_connector.py — implement run() in run.py to map SET (Social Engineering Toolkit) output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:set-social-engineering-toolkit",
  "run": "run.py",
  "mapping": "set-social-engineering-toolkit",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved SET (Social Engineering Toolkit) output file to parse instead of running it live."
    }
  ]
}
