{
  "id": "tgstat",
  "name": "TGStat",
  "type": "import",
  "category": "OSINT",
  "description": "Telegram channel analytics — post stats, audience overlap Tool: https://tgstat.com. [SCAFFOLD generated from XORCISM.TOOL #1186 by tool_to_connector.py — implement run() in run.py to map TGStat output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:tgstat",
  "run": "run.py",
  "mapping": "tgstat",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved TGStat output file to parse instead of running it live."
    }
  ]
}
