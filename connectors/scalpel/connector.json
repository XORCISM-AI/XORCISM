{
  "id": "scalpel",
  "name": "Scalpel",
  "type": "import",
  "category": "OSINT",
  "description": "File carving tool Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #863 by tool_to_connector.py — implement run() in run.py to map Scalpel output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:scalpel",
  "run": "run.py",
  "mapping": "scalpel",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Scalpel output file to parse instead of running it live."
    }
  ]
}
