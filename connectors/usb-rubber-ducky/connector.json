{
  "id": "usb-rubber-ducky",
  "name": "USB Rubber Ducky",
  "type": "import",
  "category": "OSINT",
  "description": "USB keystroke injection device Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #897 by tool_to_connector.py — implement run() in run.py to map USB Rubber Ducky output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:usb-rubber-ducky",
  "run": "run.py",
  "mapping": "usb-rubber-ducky",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved USB Rubber Ducky output file to parse instead of running it live."
    }
  ]
}
