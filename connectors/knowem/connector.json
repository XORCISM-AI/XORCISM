{
  "id": "knowem",
  "name": "KnowEm",
  "type": "import",
  "category": "OSINT",
  "description": "Username search on 500+ sites Tool: https://knowem.com/. [SCAFFOLD generated from XORCISM.TOOL #661 by tool_to_connector.py — implement run() in run.py to map KnowEm output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:knowem",
  "run": "run.py",
  "mapping": "knowem",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved KnowEm output file to parse instead of running it live."
    }
  ]
}
