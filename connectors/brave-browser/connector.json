{
  "id": "brave-browser",
  "name": "Brave Browser",
  "type": "import",
  "category": "OSINT",
  "description": "Privacy-focused browser with built-in Tor/IPFS support Tool: https://brave.com. [SCAFFOLD generated from XORCISM.TOOL #1038 by tool_to_connector.py — implement run() in run.py to map Brave Browser output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:brave-browser",
  "run": "run.py",
  "mapping": "brave-browser",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Brave Browser output file to parse instead of running it live."
    }
  ]
}
