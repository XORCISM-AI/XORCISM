{
  "id": "trufflehog",
  "name": "TruffleHog",
  "type": "import",
  "category": "Secrets",
  "description": "Finds leaked credentials across git history and many sources. Tool: https://github.com/trufflesecurity/trufflehog. [SCAFFOLD generated from XORCISM.TOOL #166 by tool_to_connector.py — implement run() in run.py to map TruffleHog output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:trufflehog",
  "run": "run.py",
  "mapping": "trufflehog",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved TruffleHog output file to parse instead of running it live."
    }
  ]
}
