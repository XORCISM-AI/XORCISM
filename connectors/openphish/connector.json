{
  "id": "openphish",
  "name": "OpenPhish",
  "type": "import",
  "category": "OSINT",
  "description": "Live phishing URL feed Tool: https://openphish.com. [SCAFFOLD generated from XORCISM.TOOL #1144 by tool_to_connector.py — implement run() in run.py to map OpenPhish output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:openphish",
  "run": "run.py",
  "mapping": "openphish",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved OpenPhish output file to parse instead of running it live."
    }
  ]
}
