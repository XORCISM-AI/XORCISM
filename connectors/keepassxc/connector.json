{
  "id": "keepassxc",
  "name": "KeePassXC",
  "type": "import",
  "category": "OSINT",
  "description": "Offline password manager Tool: https://keepassxc.org/. [SCAFFOLD generated from XORCISM.TOOL #806 by tool_to_connector.py — implement run() in run.py to map KeePassXC output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:keepassxc",
  "run": "run.py",
  "mapping": "keepassxc",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved KeePassXC output file to parse instead of running it live."
    }
  ]
}
