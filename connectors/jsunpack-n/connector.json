{
  "id": "jsunpack-n",
  "name": "Jsunpack-n",
  "type": "import",
  "category": "Honeypot",
  "description": "Jsunpack-n (Honeypot). Tool: https://github.com/urule99/jsunpack-n. [SCAFFOLD generated from XORCISM.TOOL #523 by tool_to_connector.py — implement run() in run.py to map Jsunpack-n output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:jsunpack-n",
  "run": "run.py",
  "mapping": "jsunpack-n",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Jsunpack-n output file to parse instead of running it live."
    }
  ]
}
