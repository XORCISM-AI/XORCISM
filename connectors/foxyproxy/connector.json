{
  "id": "foxyproxy",
  "name": "FoxyProxy",
  "type": "import",
  "category": "OSINT",
  "description": "Proxy management Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #928 by tool_to_connector.py — implement run() in run.py to map FoxyProxy output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:foxyproxy",
  "run": "run.py",
  "mapping": "foxyproxy",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved FoxyProxy output file to parse instead of running it live."
    }
  ]
}
