{
  "id": "telemetry",
  "name": "Telemetry",
  "type": "import",
  "category": "Social Media OSINT",
  "description": "Social Media OSINT — Telegram. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/telemetry. [SCAFFOLD generated from XORCISM.TOOL #314 by tool_to_connector.py — implement run() in run.py to map Telemetry output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:telemetry",
  "run": "run.py",
  "mapping": "telemetry",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Telemetry output file to parse instead of running it live."
    }
  ]
}
