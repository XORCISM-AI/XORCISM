{
  "id": "pacer",
  "name": "PACER",
  "type": "import",
  "category": "OSINT",
  "description": "US federal court records Tool: https://pacer.uscourts.gov/. [SCAFFOLD generated from XORCISM.TOOL #781 by tool_to_connector.py — implement run() in run.py to map PACER output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:pacer",
  "run": "run.py",
  "mapping": "pacer",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PACER output file to parse instead of running it live."
    }
  ]
}
