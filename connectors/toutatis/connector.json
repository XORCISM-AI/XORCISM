{
  "id": "toutatis",
  "name": "Toutatis",
  "type": "import",
  "category": "OSINT",
  "description": "Instagram OSINT by phone/email Tool: https://pypi.org/project/toutatis/. [SCAFFOLD generated from XORCISM.TOOL #747 by tool_to_connector.py — implement run() in run.py to map Toutatis output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:toutatis",
  "run": "run.py",
  "mapping": "toutatis",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Toutatis output file to parse instead of running it live."
    }
  ]
}
