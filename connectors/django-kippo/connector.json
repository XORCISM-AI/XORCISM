{
  "id": "django-kippo",
  "name": "Django-kippo",
  "type": "import",
  "category": "Honeypot",
  "description": "Django App for kippo SSH Honeypot. Tool: https://github.com/jedie/django-kippo. [SCAFFOLD generated from XORCISM.TOOL #626 by tool_to_connector.py — implement run() in run.py to map Django-kippo output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:django-kippo",
  "run": "run.py",
  "mapping": "django-kippo",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Django-kippo output file to parse instead of running it live."
    }
  ]
}
