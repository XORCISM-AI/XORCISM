{
  "id": "fluxion",
  "name": "Fluxion",
  "type": "import",
  "category": "OSINT",
  "description": "Wi-Fi social engineering (evil twin) Tool: https://github.com/FluxionNetwork/fluxion. [SCAFFOLD generated from XORCISM.TOOL #829 by tool_to_connector.py — implement run() in run.py to map Fluxion output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:fluxion",
  "run": "run.py",
  "mapping": "fluxion",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Fluxion output file to parse instead of running it live."
    }
  ]
}
