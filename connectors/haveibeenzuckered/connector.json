{
  "id": "haveibeenzuckered",
  "name": "haveibeenzuckered",
  "type": "import",
  "category": "OSINT",
  "description": "Check if a phone number is in the 533M Facebook breach Tool: https://haveibeenzuckered.com. [SCAFFOLD generated from XORCISM.TOOL #1103 by tool_to_connector.py — implement run() in run.py to map haveibeenzuckered output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:haveibeenzuckered",
  "run": "run.py",
  "mapping": "haveibeenzuckered",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved haveibeenzuckered output file to parse instead of running it live."
    }
  ]
}
