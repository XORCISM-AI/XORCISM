{
  "id": "whois-com",
  "name": "WHOIS.com",
  "type": "import",
  "category": "OSINT",
  "description": "WHOIS domain lookup Tool: https://www.whois.com/. [SCAFFOLD generated from XORCISM.TOOL #698 by tool_to_connector.py — implement run() in run.py to map WHOIS.com output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:whois-com",
  "run": "run.py",
  "mapping": "whois-com",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved WHOIS.com output file to parse instead of running it live."
    }
  ]
}
