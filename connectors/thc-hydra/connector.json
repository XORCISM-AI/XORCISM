{
  "id": "thc-hydra",
  "name": "THC-Hydra",
  "type": "import",
  "category": "Password Cracking",
  "description": "Fast parallelized network login brute-forcer supporting many protocols. Tool: https://github.com/vanhauser-thc/thc-hydra. [SCAFFOLD generated from XORCISM.TOOL #69 by tool_to_connector.py — implement run() in run.py to map THC-Hydra output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:thc-hydra",
  "run": "run.py",
  "mapping": "thc-hydra",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved THC-Hydra output file to parse instead of running it live."
    }
  ]
}
