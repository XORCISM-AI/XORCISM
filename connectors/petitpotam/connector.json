{
  "id": "petitpotam",
  "name": "PetitPotam",
  "type": "import",
  "category": "OSINT",
  "description": "PoC tool to coerce Windows hosts to authenticate to attacker Tool: https://github.com/topotam/PetitPotam. [SCAFFOLD generated from XORCISM.TOOL #947 by tool_to_connector.py — implement run() in run.py to map PetitPotam output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:petitpotam",
  "run": "run.py",
  "mapping": "petitpotam",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PetitPotam output file to parse instead of running it live."
    }
  ]
}
