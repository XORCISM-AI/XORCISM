{
  "id": "spot",
  "name": "SPOT",
  "type": "import",
  "category": "Geolocation & Maps OSINT",
  "description": "Geolocation & Maps OSINT. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/spot. [SCAFFOLD generated from XORCISM.TOOL #287 by tool_to_connector.py — implement run() in run.py to map SPOT output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:spot",
  "run": "run.py",
  "mapping": "spot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved SPOT output file to parse instead of running it live."
    }
  ]
}
