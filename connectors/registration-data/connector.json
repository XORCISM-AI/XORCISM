{
  "id": "registration-data",
  "name": "Registration data",
  "type": "import",
  "category": "OSINT",
  "description": "regvk.com Tool: https://regvk.com/. [SCAFFOLD generated from XORCISM.TOOL #873 by tool_to_connector.py — implement run() in run.py to map Registration data output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:registration-data",
  "run": "run.py",
  "mapping": "registration-data",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Registration data output file to parse instead of running it live."
    }
  ]
}
