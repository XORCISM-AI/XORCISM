{
  "id": "singlefile",
  "name": "SingleFile",
  "type": "import",
  "category": "OSINT",
  "description": "Save complete web pages Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #930 by tool_to_connector.py — implement run() in run.py to map SingleFile output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:singlefile",
  "run": "run.py",
  "mapping": "singlefile",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved SingleFile output file to parse instead of running it live."
    }
  ]
}
