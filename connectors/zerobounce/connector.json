{
  "id": "zerobounce",
  "name": "ZeroBounce",
  "type": "import",
  "category": "OSINT",
  "description": "Email validation and deliverability service Tool: https://www.zerobounce.net. [SCAFFOLD generated from XORCISM.TOOL #1219 by tool_to_connector.py — implement run() in run.py to map ZeroBounce output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:zerobounce",
  "run": "run.py",
  "mapping": "zerobounce",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ZeroBounce output file to parse instead of running it live."
    }
  ]
}
