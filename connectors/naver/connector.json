{
  "id": "naver",
  "name": "Naver",
  "type": "import",
  "category": "OSINT",
  "description": "South Korean search engine — local content not on Google Tool: https://www.naver.com. [SCAFFOLD generated from XORCISM.TOOL #1134 by tool_to_connector.py — implement run() in run.py to map Naver output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:naver",
  "run": "run.py",
  "mapping": "naver",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Naver output file to parse instead of running it live."
    }
  ]
}
