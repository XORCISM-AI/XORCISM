{
  "id": "owasp-webgoat",
  "name": "OWASP WebGoat",
  "type": "import",
  "category": "OSINT",
  "description": "Deliberately insecure Java-based web app for training Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #994 by tool_to_connector.py — implement run() in run.py to map OWASP WebGoat output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:owasp-webgoat",
  "run": "run.py",
  "mapping": "owasp-webgoat",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved OWASP WebGoat output file to parse instead of running it live."
    }
  ]
}
