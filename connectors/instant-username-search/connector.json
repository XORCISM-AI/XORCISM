{
  "id": "instant-username-search",
  "name": "Instant Username Search",
  "type": "import",
  "category": "OSINT",
  "description": "Real-time username checker Tool: https://instantusername.com/. [SCAFFOLD generated from XORCISM.TOOL #662 by tool_to_connector.py — implement run() in run.py to map Instant Username Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:instant-username-search",
  "run": "run.py",
  "mapping": "instant-username-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Instant Username Search output file to parse instead of running it live."
    }
  ]
}
