{
  "id": "emailheader-analyzer",
  "name": "EmailHeader-Analyzer",
  "type": "import",
  "category": "OSINT",
  "description": "CLI email header parser + OSINT Tool: https://github.com/Giritharram/EmailHeader-Analyzer-CLI-Python. [SCAFFOLD generated from XORCISM.TOOL #667 by tool_to_connector.py — implement run() in run.py to map EmailHeader-Analyzer output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:emailheader-analyzer",
  "run": "run.py",
  "mapping": "emailheader-analyzer",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved EmailHeader-Analyzer output file to parse instead of running it live."
    }
  ]
}
