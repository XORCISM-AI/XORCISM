{
  "id": "hellpot",
  "name": "HellPot",
  "type": "import",
  "category": "Honeypot",
  "description": "Honeypot that tries to crash the bots and clients that visit it's location. Tool: https://github.com/yunginnanet/HellPot. [SCAFFOLD generated from XORCISM.TOOL #357 by tool_to_connector.py — implement run() in run.py to map HellPot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:hellpot",
  "run": "run.py",
  "mapping": "hellpot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved HellPot output file to parse instead of running it live."
    }
  ]
}
