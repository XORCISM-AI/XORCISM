{
  "id": "yandex-images",
  "name": "Yandex Images",
  "type": "import",
  "category": "OSINT",
  "description": "Best reverse image search for faces/places Tool: https://yandex.com/images. [SCAFFOLD generated from XORCISM.TOOL #722 by tool_to_connector.py — implement run() in run.py to map Yandex Images output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:yandex-images",
  "run": "run.py",
  "mapping": "yandex-images",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Yandex Images output file to parse instead of running it live."
    }
  ]
}
