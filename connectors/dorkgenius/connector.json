{
  "id": "dorkgenius",
  "name": "DorkGenius",
  "type": "import",
  "category": "OSINT",
  "description": "AI-powered search query generator for Google, Bing, DuckDuckGo Tool: https://dorkgenius.com. [SCAFFOLD generated from XORCISM.TOOL #1070 by tool_to_connector.py — implement run() in run.py to map DorkGenius output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:dorkgenius",
  "run": "run.py",
  "mapping": "dorkgenius",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved DorkGenius output file to parse instead of running it live."
    }
  ]
}
