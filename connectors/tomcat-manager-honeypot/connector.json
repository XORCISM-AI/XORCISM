{
  "id": "tomcat-manager-honeypot",
  "name": "tomcat-manager-honeypot",
  "type": "import",
  "category": "Honeypot",
  "description": "Honeypot that mimics Tomcat manager endpoints. Logs requests and saves attacker's WAR file for later study. Tool: https://github.com/helospark/tomcat-manager-honeypot. [SCAFFOLD generated from XORCISM.TOOL #383 by tool_to_connector.py — implement run() in run.py to map tomcat-manager-honeypot output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:tomcat-manager-honeypot",
  "run": "run.py",
  "mapping": "tomcat-manager-honeypot",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved tomcat-manager-honeypot output file to parse instead of running it live."
    }
  ]
}
