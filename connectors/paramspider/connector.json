{
  "id": "paramspider",
  "name": "ParamSpider",
  "type": "import",
  "category": "OSINT",
  "description": "Parameter discovery from web archives Tool: https://pypi.org/project/paramspider/. [SCAFFOLD generated from XORCISM.TOOL #812 by tool_to_connector.py — implement run() in run.py to map ParamSpider output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:paramspider",
  "run": "run.py",
  "mapping": "paramspider",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ParamSpider output file to parse instead of running it live."
    }
  ]
}
