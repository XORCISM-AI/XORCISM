{
  "id": "offshore-leaks-database",
  "name": "Offshore Leaks Database",
  "type": "import",
  "category": "OSINT",
  "description": "Panama/Pandora/Paradise Papers searchable database Tool: https://offshoreleaks.icij.org. [SCAFFOLD generated from XORCISM.TOOL #1139 by tool_to_connector.py — implement run() in run.py to map Offshore Leaks Database output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:offshore-leaks-database",
  "run": "run.py",
  "mapping": "offshore-leaks-database",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Offshore Leaks Database output file to parse instead of running it live."
    }
  ]
}
