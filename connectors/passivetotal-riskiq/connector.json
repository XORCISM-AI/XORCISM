{
  "id": "passivetotal-riskiq",
  "name": "PassiveTotal (RiskIQ)",
  "type": "import",
  "category": "OSINT",
  "description": "Passive DNS, WHOIS, SSL cert pivots — now Microsoft Defender TI Tool: https://community.riskiq.com. [SCAFFOLD generated from XORCISM.TOOL #1148 by tool_to_connector.py — implement run() in run.py to map PassiveTotal (RiskIQ) output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:passivetotal-riskiq",
  "run": "run.py",
  "mapping": "passivetotal-riskiq",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved PassiveTotal (RiskIQ) output file to parse instead of running it live."
    }
  ]
}
