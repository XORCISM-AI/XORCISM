{
  "id": "kippo-stats",
  "name": "Kippo stats",
  "type": "import",
  "category": "Honeypot",
  "description": "Mojolicious app to display statistics for your kippo SSH honeypot. Tool: https://github.com/mfontani/kippo-stats. [SCAFFOLD generated from XORCISM.TOOL #641 by tool_to_connector.py — implement run() in run.py to map Kippo stats output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:kippo-stats",
  "run": "run.py",
  "mapping": "kippo-stats",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kippo stats output file to parse instead of running it live."
    }
  ]
}
