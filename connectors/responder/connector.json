{
  "id": "responder",
  "name": "Responder",
  "type": "import",
  "category": "MITM",
  "description": "LLMNR/NBT-NS/MDNS poisoner to capture NetNTLM hashes on Windows networks. Tool: https://github.com/lgandx/Responder. [SCAFFOLD generated from XORCISM.TOOL #18 by tool_to_connector.py — implement run() in run.py to map Responder output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:responder",
  "run": "run.py",
  "mapping": "responder",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Responder output file to parse instead of running it live."
    }
  ]
}
