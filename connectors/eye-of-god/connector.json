{
  "id": "eye-of-god",
  "name": "Eye of God",
  "type": "import",
  "category": "OSINT",
  "description": "Person lookup by phone/email/photo Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #872 by tool_to_connector.py — implement run() in run.py to map Eye of God output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:eye-of-god",
  "run": "run.py",
  "mapping": "eye-of-god",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Eye of God output file to parse instead of running it live."
    }
  ]
}
