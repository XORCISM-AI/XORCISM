{
  "id": "yersinia",
  "name": "Yersinia",
  "type": "import",
  "category": "Network Attacks",
  "description": "Layer-2 attack framework exploiting STP, CDP, DHCP and other protocols. Tool: https://github.com/tomac/yersinia. [SCAFFOLD generated from XORCISM.TOOL #199 by tool_to_connector.py — implement run() in run.py to map Yersinia output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": true,
  "permission": "connector:yersinia",
  "run": "run.py",
  "mapping": "yersinia",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Yersinia output file to parse instead of running it live."
    }
  ]
}
