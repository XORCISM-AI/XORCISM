{
  "id": "sec-edgar",
  "name": "SEC EDGAR",
  "type": "import",
  "category": "OSINT",
  "description": "US company filings Tool: https://sec.gov/edgar. [SCAFFOLD generated from XORCISM.TOOL #847 by tool_to_connector.py — implement run() in run.py to map SEC EDGAR output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:sec-edgar",
  "run": "run.py",
  "mapping": "sec-edgar",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved SEC EDGAR output file to parse instead of running it live."
    }
  ]
}
