{
  "id": "flickr",
  "name": "Flickr",
  "type": "import",
  "category": "OSINT",
  "description": "Photo-sharing — geosearch + license + camera EXIF intact Tool: https://www.flickr.com. [SCAFFOLD generated from XORCISM.TOOL #1082 by tool_to_connector.py — implement run() in run.py to map Flickr output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:flickr",
  "run": "run.py",
  "mapping": "flickr",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Flickr output file to parse instead of running it live."
    }
  ]
}
