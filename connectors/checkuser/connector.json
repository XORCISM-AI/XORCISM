{
  "id": "checkuser",
  "name": "CheckUser",
  "type": "import",
  "category": "OSINT",
  "description": "Search username across multiple social networks Tool: https://checkuser.vercel.app. [SCAFFOLD generated from XORCISM.TOOL #1051 by tool_to_connector.py — implement run() in run.py to map CheckUser output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:checkuser",
  "run": "run.py",
  "mapping": "checkuser",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved CheckUser output file to parse instead of running it live."
    }
  ]
}
