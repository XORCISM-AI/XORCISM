{
  "id": "haystak",
  "name": "Haystak",
  "type": "import",
  "category": "OSINT",
  "description": "Dark web search with filtering Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #787 by tool_to_connector.py — implement run() in run.py to map Haystak output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:haystak",
  "run": "run.py",
  "mapping": "haystak",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Haystak output file to parse instead of running it live."
    }
  ]
}
