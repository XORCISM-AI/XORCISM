{
  "id": "rockyou",
  "name": "RockYou",
  "type": "import",
  "category": "OSINT",
  "description": "Classic leaked password list Tool: n/a. [SCAFFOLD generated from XORCISM.TOOL #891 by tool_to_connector.py — implement run() in run.py to map RockYou output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:rockyou",
  "run": "run.py",
  "mapping": "rockyou",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved RockYou output file to parse instead of running it live."
    }
  ]
}
