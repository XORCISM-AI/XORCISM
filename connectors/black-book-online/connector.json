{
  "id": "black-book-online",
  "name": "Black Book Online",
  "type": "import",
  "category": "OSINT",
  "description": "Free nationwide directory of public-record lookups Tool: https://www.blackbookonline.info. [SCAFFOLD generated from XORCISM.TOOL #1036 by tool_to_connector.py — implement run() in run.py to map Black Book Online output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:black-book-online",
  "run": "run.py",
  "mapping": "black-book-online",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Black Book Online output file to parse instead of running it live."
    }
  ]
}
