{
  "id": "kagi-search",
  "name": "Kagi Search",
  "type": "import",
  "category": "OSINT",
  "description": "Premium privacy-respecting search engine, no ads, no tracking Tool: https://kagi.com. [SCAFFOLD generated from XORCISM.TOOL #1113 by tool_to_connector.py — implement run() in run.py to map Kagi Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:kagi-search",
  "run": "run.py",
  "mapping": "kagi-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Kagi Search output file to parse instead of running it live."
    }
  ]
}
