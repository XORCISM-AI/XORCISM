{
  "id": "faceseek",
  "name": "FaceSeek",
  "type": "import",
  "category": "OSINT",
  "description": "AI-powered reverse face search Tool: https://faceseek.online/. [SCAFFOLD generated from XORCISM.TOOL #732 by tool_to_connector.py — implement run() in run.py to map FaceSeek output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:faceseek",
  "run": "run.py",
  "mapping": "faceseek",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved FaceSeek output file to parse instead of running it live."
    }
  ]
}
