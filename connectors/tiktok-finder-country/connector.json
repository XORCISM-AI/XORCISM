{
  "id": "tiktok-finder-country",
  "name": "TikTok Finder Country",
  "type": "import",
  "category": "OSINT",
  "description": "Free OSINT lookup for TikTok account country/language Tool: https://tiktokfindercountry.xyz. [SCAFFOLD generated from XORCISM.TOOL #1189 by tool_to_connector.py — implement run() in run.py to map TikTok Finder Country output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:tiktok-finder-country",
  "run": "run.py",
  "mapping": "tiktok-finder-country",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved TikTok Finder Country output file to parse instead of running it live."
    }
  ]
}
