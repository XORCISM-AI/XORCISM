{
  "id": "nozomi",
  "name": "Nozomi Networks",
  "type": "import",
  "category": "OT/IoT",
  "description": "Nozomi Networks (Guardian / Vantage) OT/IoT visibility & detection. Parses an exported assets + vulnerabilities report (JSON or CSV) and maps each node to an ASSET and each vulnerability/CVE to a finding on that asset. Tool: https://www.nozominetworks.com. Offline export parser (live Vantage/Guardian API via NOZOMI_URL + NOZOMI_API_KEY is a future enhancement).",
  "intrusive": false,
  "permission": "connector:nozomi",
  "run": "run.py",
  "mapping": "nozomi",
  "parameters": [
    {
      "name": "file",
      "type": "file",
      "required": true,
      "help": "Nozomi export to parse: JSON ({nodes:[…], vulnerabilities:[…]} or a list), or a CSV of assets or of vulnerabilities (auto-detected by columns)."
    },
    {
      "name": "project",
      "type": "string",
      "required": false,
      "help": "Optional asset-name override applied to all findings (otherwise each node's label / IP is used)."
    }
  ]
}
