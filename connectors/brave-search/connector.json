{
  "id": "brave-search",
  "name": "Brave Search",
  "type": "import",
  "category": "OSINT",
  "description": "Independent, transparent, ad-free search engine Tool: https://search.brave.com. [SCAFFOLD generated from XORCISM.TOOL #1039 by tool_to_connector.py — implement run() in run.py to map Brave Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:brave-search",
  "run": "run.py",
  "mapping": "brave-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Brave Search output file to parse instead of running it live."
    }
  ]
}
