{
  "id": "govsky",
  "name": "Govsky",
  "type": "import",
  "category": "Social Media OSINT",
  "description": "Social Media OSINT — Bluesky. OSINT tool from the OSINT Newsletter tools library (https://tools.osintnewsletter.com/). Tool: https://tools.osintnewsletter.com/osint-tools/govsky. [SCAFFOLD generated from XORCISM.TOOL #317 by tool_to_connector.py — implement run() in run.py to map Govsky output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:govsky",
  "run": "run.py",
  "mapping": "govsky",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Govsky output file to parse instead of running it live."
    }
  ]
}
