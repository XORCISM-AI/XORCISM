"""run.py — XORCISM connector SCAFFOLD for ParamMiner (Burp ext).

Discover hidden, unlinked HTTP parameters in Burp
Tool: https://github.com/PortSwigger/param-miner
Category: OSINT   (XORCISM.TOOL #945)

Generated by connectors/tool_to_connector.py. Implement run() to map ParamMiner (Burp ext) output to
the normalized XORCISM result:
    {"assets": [{"hostname": str, "key": str, "ip": str?, "os": str?}],
     "services": [], "cpes": [], "vulns": [{"asset": str, "ref": str, "name": str, "severity": str}]}
The runner (connectors/runner.py) imports that result. NO database access here (so the
connector also runs on a remote worker).
"""
from __future__ import annotations

from typing import Any, Dict

TOOL_NAME = 'ParamMiner (Burp ext)'
TOOL_URL = 'https://github.com/PortSwigger/param-miner'


def run(params: Dict[str, Any], workdir: str) -> Dict[str, Any]:  # noqa: ARG001
    target = str(params.get("target") or "").strip()
    path = params.get("file")
    # TODO: implement the ParamMiner (Burp ext) integration:
    #   • live mode  -> invoke ParamMiner (Burp ext) against `target` (respect the engagement scope), or
    #   • offline    -> parse the saved output at `path`,
    # then return the normalized result (example shape below).
    raise NotImplementedError(
        f"Connector 'paramminer-burp-ext' ({TOOL_NAME}) is a scaffold - implement run() in "
        f"connectors/paramminer-burp-ext/run.py (tool: {TOOL_URL})."
    )
    # return {"assets": [], "services": [], "cpes": [], "vulns": []}


if __name__ == "__main__":
    import json as _json
    import tempfile
    print(_json.dumps(run({"target": "", "file": None}, tempfile.mkdtemp())))
