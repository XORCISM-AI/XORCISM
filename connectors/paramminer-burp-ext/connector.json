{
  "id": "paramminer-burp-ext",
  "name": "ParamMiner (Burp ext)",
  "type": "import",
  "category": "OSINT",
  "description": "Discover hidden, unlinked HTTP parameters in Burp Tool: https://github.com/PortSwigger/param-miner. [SCAFFOLD generated from XORCISM.TOOL #945 by tool_to_connector.py — implement run() in run.py to map ParamMiner (Burp ext) output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:paramminer-burp-ext",
  "run": "run.py",
  "mapping": "paramminer-burp-ext",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved ParamMiner (Burp ext) output file to parse instead of running it live."
    }
  ]
}
