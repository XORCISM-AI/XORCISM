{
  "id": "unicorn-engine",
  "name": "Unicorn Engine",
  "type": "import",
  "category": "IoT",
  "description": "Lightweight multi-architecture CPU emulator framework used to run/firmware-emulate code. Tool: https://github.com/unicorn-engine/unicorn. [SCAFFOLD generated from XORCISM.TOOL #1223 by tool_to_connector.py — implement run() in run.py to map Unicorn Engine output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:unicorn-engine",
  "run": "run.py",
  "mapping": "unicorn-engine",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Unicorn Engine output file to parse instead of running it live."
    }
  ]
}
