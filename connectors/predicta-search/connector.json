{
  "id": "predicta-search",
  "name": "Predicta Search",
  "type": "import",
  "category": "OSINT",
  "description": "Search social accounts by email or phone Tool: https://predictasearch.com. [SCAFFOLD generated from XORCISM.TOOL #1154 by tool_to_connector.py — implement run() in run.py to map Predicta Search output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:predicta-search",
  "run": "run.py",
  "mapping": "predicta-search",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Predicta Search output file to parse instead of running it live."
    }
  ]
}
