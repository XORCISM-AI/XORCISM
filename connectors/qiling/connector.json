{
  "id": "qiling",
  "name": "Qiling",
  "type": "import",
  "category": "IoT",
  "description": "Cross-platform, multi-architecture binary emulation and instrumentation framework. Tool: https://github.com/qilingframework/qiling. [SCAFFOLD generated from XORCISM.TOOL #1222 by tool_to_connector.py — implement run() in run.py to map Qiling output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:qiling",
  "run": "run.py",
  "mapping": "qiling",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved Qiling output file to parse instead of running it live."
    }
  ]
}
