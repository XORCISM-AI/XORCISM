{
  "id": "socradar",
  "name": "SOCRadar",
  "type": "import",
  "category": "OSINT",
  "description": "Extended threat intel — attack surface + dark web + brand Tool: https://socradar.io. [SCAFFOLD generated from XORCISM.TOOL #969 by tool_to_connector.py — implement run() in run.py to map SOCRadar output to the XORCISM findings model {assets, services, cpes, vulns}.]",
  "intrusive": false,
  "permission": "connector:socradar",
  "run": "run.py",
  "mapping": "socradar",
  "parameters": [
    {
      "name": "target",
      "type": "string",
      "required": false,
      "help": "Host, URL or path the tool acts on. When implementing, consider type 'target' or 'url' so the runner enforces the engagement scope."
    },
    {
      "name": "file",
      "type": "file",
      "required": false,
      "help": "Offline mode: a saved SOCRadar output file to parse instead of running it live."
    }
  ]
}
